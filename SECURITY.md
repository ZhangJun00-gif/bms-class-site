# 数据与安全边界

## 发布物与运行数据

本源码发布包不带任何源站业务数据库、用户账号、私有配置、对象存储数据或原 Git 历史。schema、迁移、接口与字段定义属于软件功能，保留它们不等于发布既有记录。

部署后，下列内容均需由部署者按个人信息或私有业务数据处理：

| 范围 | 实际内容 | 存储与外发 |
| --- | --- | --- |
| 用户与认证 | 显示姓名、登录标识的哈希与密文、密码哈希、账号状态、会话与 CSRF 数据 | MySQL；一次性邀请码和临时密码在界面返回，避免截屏共享 |
| 动态、公告、论坛 | 作者标识、自由文本、发布时间、阅读记录、举报和管理操作 | MySQL；被管理员设为公开的动态可对匿名访客展示 |
| 相册和媒体 | 人脸、活动地点、文件名、图片内容、原图 EXIF、对象键 | COS；展示图处理不能代替原图去标识，原图按原始字节保留 |
| 学时 | 姓名、活动名称、时长、凭证截图、审核理由、排行榜与导出 | MySQL / COS；AI 复核会将凭证原图和显示姓名发送给 DeepSeek 文件与模型接口 |
| 知识和题库 | 私有教材、导入原文件、文档版本、题干答案、解析、资源关系与索引 | MySQL / uploads / COS / Qdrant；允许外发的知识片段与查询会到 Embedding / AI |
| 练习和 AI | 学习轨迹、答案、分数、错题、掌握状态、建议、对话全文、AI 调用及使用量 | MySQL；每日计划使用匿名别名，但自由文本和其他 AI 任务不保证匿名 |
| 运维和备份 | 日志、故障详情、数据库转储、对象副本、快照、配置与 TLS 私钥 | 受限配置目录和私有备份；禁止放在源码、公共静态目录或公开 CI artifact |

学号的无盐 SHA-256 检索值不是匿名数据：对于规律编号，数据库读取者可枚举候选值。AES-GCM 加密也依赖加密密钥保密。请将数据库与配置备份都视为敏感材料，减少真实标识收集，限制数据库管理权限。

## 第三方与权限

- COS 桶必须私有，使用专用限权凭据；每个部署独立桶或明确隔离的存储空间，避免共享可删除其他班级对象的权限。
- 签名 URL 是短期访问凭证。公开日志和报错截图不得带签名参数、原图地址、Cookie 或 Authorization。
- AI 及 Embedding key 只进入后端环境文件，不能使用 `VITE_*` 注入前端，避免泄露。
- 私有知识库可以关闭 AI 外发。学时 AI 原图复核有独立外发流程，启用业务前应告知成员并明确处理规则。
- AI 供应商文件有删除重试与状态记录；存在失败与到期状态，不能把调用完成当作供应商已彻底删除所有副本。
- Docker 管理权限等价于高权限访问运行秘密。`docker inspect`、`docker compose config` 和容器环境转储都不可直接公开。使用 `config --quiet` 只验证结构。
- 源码默认不提供认证公网渗透测试、第三方数据保留政策核验或高并发容量认证。

## 备份与恢复

接收真实成员数据之前，应建立受限备份目录、加密离线副本和告警，并在隔离环境完成一次恢复：

1. 备份完整 MySQL 业务库和迁移状态，记录对应应用版本。转储中包含密码哈希和个人数据。
2. 备份持久 `uploads` 卷，保留知识与题库导入原文件所需内容。
3. 单独备份 COS 中的处理图、相册原图和学时凭证原图；数据库中的对象键清单不等于对象备份。考虑对象版本控制或离线副本。
4. 备份 Qdrant 快照、集合与 alias 配置；记录 embedding 模型和维度。
5. 加密备份私有运行配置，尤其是 `STUDENT_DATA_KEY`。该值丢失后无法解密已有学生标识，直接换值会破坏已有数据。
6. 将上述备份恢复到独立的数据库、上传卷、对象空间和向量实例，核对数据库/原文件/媒体/密钥匹配，执行实际读取检查。恢复操作不得覆盖其他用户新增数据。

本首次安装脚本不会创建备份计划，也不包含数据清空、删卷或强制回滚命令。运营者需要自己承担备份调度、容量、保留期限、恢复时间和访问控制。

## 报告问题

发现安全问题时，先向部署负责人提交最小复现。不要将真实信息、真实凭证、供应商 key、生产日志或可用签名链接放入公开 issue。
