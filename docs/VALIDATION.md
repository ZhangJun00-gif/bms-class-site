# 验证说明

本地验证完成于 2026-09-08，2026-09-11 复核。对象是本仓库源码，不是任何在线站点。

## 本地已验证

| 检查 | 结果 | 范围 |
| --- | --- | --- |
| Prisma Client 生成、全工作区类型检查 | 通过 | 未连接数据库 |
| 全工作区生产构建 | 通过 | API、Worker、Web 与共享包；Web 不生成 source map |
| 自动化回归 | 通过 | OpenAPI、迁移与管理员初始化 21 项；部署安全 6 项；认证与运行配置检查 |
| 依赖审计 | `pnpm audit` 0 条已知命中 | registry 公告快照，不含未知漏洞 |
| Compose 结构 | 官方 Compose 2.39.4 `config --quiet` 通过 | 只解析合成配置，未启动容器 |
| 本地 GUI | 桌面 1440×1000、手机 390×844、平板 768×1024 | 本地构建产物与空数据 HTTP 替身 |

GUI 检查覆盖首页图标与主视觉、匿名练习入口跳转登录页、手机登录与申请表单、手机导航、平板动态空状态；受检页面无横向溢出，图标加载正常，控制台无异常日志。

## 部署时需要在目标环境验证

- Linux 服务器镜像构建、首次空库迁移、卷权限与安装中断后的恢复。
- COS 私有桶读写、签名访问、原图导出与删除补偿。
- AI / Embedding 账户的模型权限、文件接口、额度与数据处理政策。
- 管理员登录、成员审批、刷新持久化、后台任务完成与各业务权限。
- 域名 DNS、证书链、外部网络访问、证书续期与防火墙规则。
- 目标容量下的图片、文档、题库与并发处理。
- 数据库、原文件、COS、Qdrant 与数据密钥的隔离恢复。
