openapi: 3.1.0
info:
  title: 班级网站网站 API
  version: 0.1.0
servers:
  - url: /api/v1
security:
  - sessionCookie: []
paths:
  /health:
    get:
      security: []
      summary: 获取不包含运行时配置的 API 健康状态
      responses:
        "200":
          description: API 正常运行
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HealthResponse" }
  /health/ready:
    get:
      security: []
      summary: 检查数据库、Qdrant 物理集合和精确 alias 映射
      responses:
        "200":
          description: API 已具备接收业务流量的全部必要依赖
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ReadinessResponse" }
        "503":
          description: 至少一个必要依赖或运行配置未就绪
  /auth/register:
    post:
      security: []
      summary: 使用邀请码申请账号
      responses:
        "201": { description: 申请已提交 }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /auth/login:
    post:
      security: []
      summary: 登录并创建安全会话
      responses:
        "200":
          description: 登录成功
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthSession" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /auth/me:
    get:
      summary: 获取当前成员和 CSRF token
      responses:
        "200":
          description: 当前成员
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthSession" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /auth/logout:
    post:
      summary: 注销当前会话
      responses:
        "204": { description: 已注销 }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /auth/change-password:
    post:
      summary: 修改当前成员密码并失效其他设备会话
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ChangePasswordRequest" }
      responses:
        "201":
          description: 密码已更新
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessageResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /invites:
    get:
      summary: 列出邀请码元数据，不返回哈希或明文邀请码
      x-required-roles: [ADMIN]
      responses:
        "200":
          description: 邀请码列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InviteListResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: 生成一个仅显示一次的邀请码
      x-required-roles: [ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateInviteRequest" }
      responses:
        "201":
          description: 邀请码已生成，code 仅在本次响应显示
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CreateInviteResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /invites/{id}/revoke:
    post:
      summary: 幂等撤销邀请码，不再次返回明文
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201":
          description: 邀请码已撤销或此前已经撤销
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InviteItem" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users:
    get:
      summary: 获取成员列表
      x-required-roles: [ADMIN]
      responses:
        "200":
          description: 成员列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MemberList" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /users/{id}:
    patch:
      summary: 更新成员状态或角色
      description: 不能停用或降级当前登录管理员，也不能停用或降级最后一个 ACTIVE ADMIN。
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateUserRequest" }
      responses:
        "200":
          description: 已更新
          content:
            application/json:
              schema: { $ref: "#/components/schemas/User" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /users/{id}/reset-password:
    post:
      summary: 重置非当前管理员的成员密码并失效其会话
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201":
          description: 临时密码仅在本次响应显示
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ResetPasswordResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /admin/audit-logs:
    get:
      summary: 按复合游标和条件筛选审计日志
      x-required-roles: [ADMIN]
      parameters:
        - in: query
          name: cursor
          schema: { type: string, maxLength: 512 }
        - in: query
          name: pageSize
          schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
        - in: query
          name: actorId
          schema: { type: string, maxLength: 191 }
        - in: query
          name: action
          schema: { type: string, maxLength: 100 }
        - in: query
          name: targetType
          schema: { type: string, maxLength: 80 }
        - in: query
          name: targetId
          schema: { type: string, maxLength: 64 }
        - in: query
          name: from
          schema: { type: string, format: date-time }
        - in: query
          name: to
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: 审计日志列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuditLogPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /admin/audit-logs.csv:
    get:
      summary: 按与列表相同的条件导出有界审计 CSV
      description: 最多导出 5000 条；X-Result-Truncated 明确标识是否仍有更多匹配记录。
      x-required-roles: [ADMIN]
      parameters:
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 5000, default: 5000 }
        - in: query
          name: actorId
          schema: { type: string, maxLength: 191 }
        - in: query
          name: action
          schema: { type: string, maxLength: 100 }
        - in: query
          name: targetType
          schema: { type: string, maxLength: 80 }
        - in: query
          name: targetId
          schema: { type: string, maxLength: 64 }
        - in: query
          name: from
          schema: { type: string, format: date-time }
        - in: query
          name: to
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: UTF-8 BOM CSV
          headers:
            X-Result-Limit:
              schema: { type: integer }
            X-Result-Truncated:
              schema: { type: boolean }
          content:
            text/csv:
              schema: { type: string }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /news:
    get:
      security: []
      summary: 分页获取公开动态
      description: includeBody 默认为 true 以兼容旧前端；新前端应传 false 并通过详情接口按需读取正文。
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
        - $ref: "#/components/parameters/IncludeNewsBody"
      responses:
        "200":
          description: 动态列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NewsPage" }
    post:
      summary: 新建动态
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NewsWriteRequest" }
      responses:
        "201":
          description: 已创建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NewsRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /news/members:
    get:
      summary: 分页获取成员可见的已发布动态
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
        - $ref: "#/components/parameters/IncludeNewsBody"
      responses:
        "200":
          description: 动态列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NewsPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /news/members/{id}:
    get:
      summary: 获取成员可见的动态详情
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 动态详情
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NewsItem" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /news/drafts:
    get:
      summary: 获取当前编辑者自己的动态草稿
      x-required-roles: [EDITOR, ADMIN]
      responses:
        "200":
          description: 草稿列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NewsList" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /news/drafts/{id}:
    get:
      summary: 获取当前编辑者自己的一篇动态草稿
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 草稿详情
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NewsItem" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /news/manage:
    get:
      summary: 分页获取可管理的动态
      description: EDITOR 只能查看自己创建的动态；ADMIN 可查看所有未删除动态。
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
        - in: query
          name: status
          schema: { $ref: "#/components/schemas/ContentStatus" }
      responses:
        "200":
          description: 可管理动态列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NewsManagePage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /news/manage/{id}:
    get:
      summary: 获取一篇可管理动态的详情
      description: EDITOR 只能读取自己创建的动态；ADMIN 可读取所有未删除动态。
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 管理动态详情
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NewsItem" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /news/images:
    post:
      summary: 上传动态图片并压缩为不超过 300KB 的 WebP
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file: { type: string, format: binary }
                albumId: { type: string, description: 同时加入的相册 ID }
                caption: { type: string, maxLength: 300 }
      responses:
        "201": { description: 图片已存入数据库 }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /news/{id}:
    get:
      security: []
      summary: 获取公开动态详情
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 动态详情
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NewsItem" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: 更新未归档动态
      description: EDITOR 只能修改自己创建的动态；ADMIN 可修改其他人的动态。expectedUpdatedAt 不匹配时返回 409。
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NewsUpdateRequest" }
      responses:
        "200":
          description: 已更新
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NewsRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
    delete:
      summary: 软删除动态并释放正文图片关系
      description: EDITOR 只能删除自己创建的动态；ADMIN 可删除其他人的动态。图片对象由现有孤立媒体回收流程按宽限期处理。
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 动态已软删除
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NewsDeleteResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /news/{id}/archive:
    patch:
      summary: 归档动态
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NewsVersionRequest" }
      responses:
        "200":
          description: 动态已归档
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NewsRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /news/{id}/restore:
    patch:
      summary: 将已归档动态恢复为草稿
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NewsVersionRequest" }
      responses:
        "200":
          description: 动态已恢复为草稿
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NewsRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /media/images/{id}/content:
    get:
      summary: 读取旧数据库图片或重定向到私有 COS；公开动态引用的图片允许匿名访问
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200": { description: 旧数据库图片内容 }
        "302": { description: 私有 COS 的短期签名读取地址 }
        "404": { $ref: "#/components/responses/NotFound" }
        "503": { description: 图片存储服务暂时不可用 }
  /media/images/orphans:
    get:
      summary: 只读列出没有相册、动态或题目关联的图片元数据
      x-required-roles: [ADMIN]
      responses:
        "200":
          description: 孤立图片报告；接口不会删除任何记录或对象
          content:
            application/json:
              schema:
                type: object
                required: [generatedAt, total, totalBytes, items, cosComparison]
                properties:
                  generatedAt: { type: string, format: date-time }
                  total: { type: integer, minimum: 0 }
                  totalBytes: { type: integer, minimum: 0 }
                  items:
                    type: array
                    items:
                      type: object
                      required: [id, objectKey, size, createdAt, uploadedById]
                      properties:
                        id: { type: string }
                        objectKey: { type: [string, "null"] }
                        size: { type: integer, minimum: 0 }
                        createdAt: { type: string, format: date-time }
                        uploadedById: { type: [string, "null"] }
                  cosComparison:
                    description: database 模式为 null；COS 模式对账 media/ 前缀与 Photo.objectKey。
                    oneOf:
                      - type: "null"
                      - type: object
                        required:
                          [objectCount, untrackedObjects, missingObjects]
                        properties:
                          objectCount: { type: integer, minimum: 0 }
                          untrackedObjects:
                            type: array
                            items:
                              type: object
                              required: [key, size, lastModified]
                              properties:
                                key: { type: string }
                                size: { type: integer, minimum: 0 }
                                lastModified:
                                  { type: string, format: date-time }
                          missingObjects:
                            type: array
                            items:
                              type: object
                              required: [id, objectKey]
                              properties:
                                id: { type: string }
                                objectKey: { type: string }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /albums:
    get:
      summary: 获取成员相册
      description: 默认只返回未归档相册摘要；includePhotos=true 保留旧客户端全量读取兼容，管理端可用 includeArchived=true 查看归档记录。
      parameters:
        - in: query
          name: includePhotos
          required: false
          schema: { type: boolean, default: false }
        - in: query
          name: includeArchived
          required: false
          description: 仅 EDITOR/ADMIN 可设为 true。
          schema: { type: boolean, default: false }
      responses:
        "200":
          description: 相册列表（默认摘要，includePhotos=true 时为全量）
          content:
            application/json:
              schema:
                oneOf:
                  - { $ref: "#/components/schemas/AlbumSummaryListResponse" }
                  - { $ref: "#/components/schemas/AlbumListResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: 创建相册
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AlbumWriteRequest" }
      responses:
        "201":
          description: 相册已创建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AlbumRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /albums/{id}:
    get:
      summary: 兼容获取单个相册的完整照片列表
      description: 普通成员不可读取归档相册；新客户端应使用照片游标接口懒加载。
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 相册详情（含全部照片）
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AlbumItem" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: 重命名相册
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AlbumRenameRequest" }
      responses:
        "200":
          description: 相册已重命名；照片关系保持不变
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AlbumRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /albums/{id}/archive:
    post:
      summary: 幂等归档相册并保留全部照片关系
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201":
          description: 相册已归档或此前已经归档
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AlbumRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /albums/{id}/restore:
    post:
      summary: 幂等恢复已归档相册及其照片关系
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201":
          description: 相册已恢复或此前已经恢复
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AlbumRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /albums/{id}/photos:
    get:
      summary: 按稳定游标分页读取相册照片
      description: 普通成员不可读取归档相册；EDITOR/ADMIN 可预览归档相册。
      parameters:
        - $ref: "#/components/parameters/Id"
        - in: query
          name: cursor
          schema: { type: string, maxLength: 512 }
        - in: query
          name: pageSize
          schema: { type: integer, minimum: 1, maximum: 100, default: 30 }
      responses:
        "200":
          description: 照片游标页
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AlbumPhotoPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }
    post:
      summary: 向已有相册上传图片
      description: 同时保留逐字节原图与独立 WebP 展示图；JPEG/PNG/WebP，最大 10 MiB、3200 万像素。同键同内容重试返回原记录，不同内容返回 409。
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file: { type: string, format: binary }
                caption: { type: string, maxLength: 300 }
      responses:
        "201":
          description: 图片已加入相册
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PhotoItem" }
        "400": { description: 图片格式、大小或内容不符合要求 }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /albums/{albumId}/photos/{photoId}:
    delete:
      summary: 从相册移除图片；无其他业务引用时同时删除图片记录和存储对象
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: albumId
          required: true
          schema: { type: string }
        - in: path
          name: photoId
          required: true
          schema: { type: string }
      responses:
        "200":
          description: 已从相册移除
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AlbumPhotoRemoveResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /albums/photos/{id}/content:
    get:
      deprecated: true
      summary: 兼容读取相册图片内容
      description: 兼容旧客户端的认证读取路由；新客户端应使用 /media/images/{id}/content。
      x-deprecation-candidate: true
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 旧数据库图片内容
          content:
            image/webp:
              schema: { type: string, format: binary }
        "302":
          description: 私有 COS 的短期签名读取地址
          headers:
            Location:
              schema: { type: string, format: uri }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "503": { description: 图片存储服务暂时不可用 }

  /forum/threads:
    get:
      summary: 获取未隐藏、未删除的论坛主题
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: 主题列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ForumThreadList" }
    post:
      summary: 发布论坛主题
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ForumThreadWriteRequest" }
      responses:
        "201":
          description: 已发布
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ForumThreadRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /forum/threads/{id}:
    get:
      summary: 获取主题和未隐藏回复
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 主题详情
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ForumThread" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: 编辑主题；MEMBER 仅可编辑自己的主题
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ForumThreadWriteRequest" }
      responses:
        "200":
          description: 已更新
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ForumThreadRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }
    delete:
      summary: 删除主题；MEMBER 仅可删除自己的主题
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200": { description: 已删除 }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /forum/threads/{id}/posts:
    post:
      summary: 回复未锁定主题
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ForumPostWriteRequest" }
      responses:
        "201":
          description: 已回复
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ForumPostRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /forum/posts/{id}:
    patch:
      summary: 编辑回复；MEMBER 仅可编辑自己的回复
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ForumPostWriteRequest" }
      responses:
        "200":
          description: 已更新
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ForumPostRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }
    delete:
      summary: 删除回复；MEMBER 仅可删除自己的回复
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200": { description: 已删除 }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /forum/threads/{id}/reports:
    post:
      summary: 举报主题
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ForumReportWriteRequest" }
      responses:
        "201": { description: 已举报 }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /forum/posts/{id}/reports:
    post:
      summary: 举报回复
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ForumReportWriteRequest" }
      responses:
        "201": { description: 已举报 }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /forum/reports:
    get:
      summary: 获取待处理举报
      x-required-roles: [EDITOR, ADMIN]
      responses:
        "200":
          description: 待处理举报列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ForumReportList" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /forum/reports/{id}/resolve:
    patch:
      summary: 将举报标记为已处理
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 已处理
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ForumReportRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /forum/threads/{id}/moderation:
    patch:
      summary: 设置主题置顶、锁定或隐藏状态
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ForumModerationRequest" }
      responses:
        "200":
          description: 已更新审核状态
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ForumThreadRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /knowledge:
    get:
      summary: 获取已发布知识资料
      parameters:
        - in: query
          name: subjectId
          schema: { type: string }
      responses:
        "200":
          description: 按学科筛选的资料列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeListResponse" }
  /subjects:
    get:
      summary: 获取共享学科目录
      parameters:
        - in: query
          name: includeInactive
          description: 仅 EDITOR/ADMIN 可查看停用项
          schema: { type: boolean, default: false }
      responses:
        "200":
          description: 学科目录
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Subject" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: 创建共享学科
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DirectoryCreateRequest" }
      responses:
        "201":
          description: 学科已创建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Subject" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /subjects/{id}:
    patch:
      summary: 更新、排序或停用学科
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DirectoryUpdateRequest" }
      responses:
        "200":
          description: 学科已更新
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Subject" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /subjects/{id}/chapters:
    get:
      summary: 获取指定学科的章节目录
      parameters:
        - $ref: "#/components/parameters/Id"
        - in: query
          name: includeInactive
          description: 仅 EDITOR/ADMIN 可查看停用项
          schema: { type: boolean, default: false }
      responses:
        "200":
          description: 章节目录
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SubjectChapter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: 在指定学科下创建章节
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DirectoryCreateRequest" }
      responses:
        "201":
          description: 章节已创建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SubjectChapter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /subjects/{subjectId}/chapters/{chapterId}:
    patch:
      summary: 更新、排序或停用学科章节
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: subjectId
          required: true
          schema: { type: string }
        - in: path
          name: chapterId
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DirectoryUpdateRequest" }
      responses:
        "200":
          description: 章节已更新
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SubjectChapter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /knowledge/subjects:
    get:
      summary: 获取启用的知识库学科
      responses:
        "200":
          description: 学科列表
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/KnowledgeSubject" }
    post:
      summary: 创建知识库学科
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              { $ref: "#/components/schemas/KnowledgeSubjectCreateRequest" }
      responses:
        "201":
          description: 学科已创建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeSubject" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /knowledge/libraries:
    get:
      summary: 分页获取当前成员可访问的共享与本人私有知识库
      parameters:
        - in: query
          name: scope
          schema: { $ref: "#/components/schemas/KnowledgeLibraryScope" }
        - in: query
          name: subjectId
          schema: { type: string }
        - in: query
          name: query
          schema: { type: string, minLength: 1, maxLength: 160 }
        - { $ref: "#/components/parameters/Page" }
        - in: query
          name: pageSize
          schema: { type: integer, minimum: 1, maximum: 50, default: 20 }
      responses:
        "200":
          description: 已按服务端租户权限过滤
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeLibraryDirectory" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: 创建共享或本人私有知识库
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KnowledgeLibraryCreateRequest" }
      responses:
        "201":
          description: 知识库已创建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeLibrary" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /knowledge/libraries/{id}:
    get:
      summary: 获取可访问知识库元数据
      parameters: [{ $ref: "#/components/parameters/Id" }]
      responses:
        "200":
          description: 知识库元数据
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeLibrary" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: 重命名或停用可管理知识库
      parameters: [{ $ref: "#/components/parameters/Id" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                name: { type: string, minLength: 2, maxLength: 160 }
                active: { type: boolean }
      responses:
        "200": { description: 知识库已更新 }
        "403": { $ref: "#/components/responses/Forbidden" }
    delete:
      summary: 软删除知识库并排队清理向量和图片
      parameters: [{ $ref: "#/components/parameters/Id" }]
      responses:
        "200": { description: 知识库已失效，返回 cleanupPending }
        "403": { $ref: "#/components/responses/Forbidden" }
  /knowledge/libraries/{id}/ai-settings:
    patch:
      summary: 设置本人私有知识库的 AI 使用开关
      parameters: [{ $ref: "#/components/parameters/Id" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [enabled, acknowledged]
              properties:
                enabled: { type: boolean }
                acknowledged: { type: boolean }
      responses:
        "200": { description: AI 设置已更新 }
        "403": { $ref: "#/components/responses/Forbidden" }
  /knowledge/libraries/{libraryId}/chapters:
    get:
      summary: 分页获取当前成员可访问知识库的启用章节
      parameters:
        - in: path
          name: libraryId
          required: true
          schema: { type: string }
        - { $ref: "#/components/parameters/Page" }
        - in: query
          name: pageSize
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: 章节选项分页
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeLibraryChapterPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /knowledge/libraries/{libraryId}/reader:
    get:
      summary: 获取不含 H1 和文档切换信息的连续知识库 Reader 清单
      parameters:
        - in: path
          name: libraryId
          required: true
          schema: { type: string }
      responses:
        "200":
          description: 当前活动版本快照清单
          headers:
            ETag: { schema: { type: string } }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeReaderManifest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /knowledge/libraries/{libraryId}/reader/outline:
    get:
      summary: 分页获取连续知识库 H2-H6 大纲
      parameters:
        - in: path
          name: libraryId
          required: true
          schema: { type: string }
        - in: query
          name: revision
          required: true
          schema: { type: string, minLength: 10, maxLength: 100 }
        - in: query
          name: cursor
          schema: { type: string, minLength: 10, maxLength: 100 }
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 200, default: 100 }
      responses:
        "200":
          description: 大纲分页
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeReaderOutlineResponse" }
        "409": { $ref: "#/components/responses/Conflict" }
  /knowledge/libraries/{libraryId}/reader/range:
    get:
      summary: 以不透明锚点双向获取有界连续正文范围
      parameters:
        - in: path
          name: libraryId
          required: true
          schema: { type: string }
        - in: query
          name: revision
          required: true
          schema: { type: string, minLength: 10, maxLength: 100 }
        - in: query
          name: anchor
          schema: { type: string, minLength: 1, maxLength: 191 }
        - in: query
          name: before
          schema: { type: integer, minimum: 0, maximum: 40, default: 8 }
        - in: query
          name: after
          schema: { type: integer, minimum: 0, maximum: 40, default: 16 }
      responses:
        "200":
          description: 最多 60 项且 Markdown 总计不超过 256 KiB 的范围
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeReaderRangeResponse" }
        "409": { $ref: "#/components/responses/Conflict" }
  /knowledge/libraries/{libraryId}/reader-context:
    get:
      summary: 重新授权并将节点或正文块解析为当前连续 Reader 锚点
      parameters:
        - in: path
          name: libraryId
          required: true
          schema: { type: string }
        - in: query
          name: revision
          schema: { type: string, minLength: 10, maxLength: 100 }
        - in: query
          name: nodeId
          schema: { type: string, minLength: 1, maxLength: 191 }
        - in: query
          name: blockId
          schema: { type: string, minLength: 1, maxLength: 191 }
      responses:
        "200":
          description: 当前活动版本中的授权锚点和标题路径
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeReaderContext" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /knowledge/private-library-metadata:
    get:
      summary: 获取私有知识库元数据和计数，不返回正文、替代文本或图片
      x-required-roles: [ADMIN]
      responses:
        "200": { description: 私有库元数据分页 }
        "403": { $ref: "#/components/responses/Forbidden" }
  /knowledge/private-library-metadata/{id}/status:
    patch:
      summary: 管理员停用或恢复私有知识库
      x-required-roles: [ADMIN]
      parameters: [{ $ref: "#/components/parameters/Id" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [active]
              properties: { active: { type: boolean } }
      responses:
        "200": { description: 状态已更新 }
        "403": { $ref: "#/components/responses/Forbidden" }
  /knowledge/imports:
    get:
      summary: 获取当前成员可管理的知识导入任务
      parameters:
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PageSize" }
      responses:
        "200":
          description: 导入任务分页
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeImportJobPage" }
    post:
      summary: 上传 Markdown 或最大 200 MiB 的 ZIP 知识包
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, libraryId]
              properties:
                file: { type: string, format: binary }
                libraryId: { type: string }
                targetDocumentId: { type: string }
      responses:
        "202":
          description: 已进入异步预检队列
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeImportJob" }
        "413": { description: 文件超过限制 }
  /knowledge/imports/{id}:
    get:
      summary: 获取知识导入任务
      parameters: [{ $ref: "#/components/parameters/Id" }]
      responses:
        "200":
          description: 导入任务
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeImportJob" }
    delete:
      summary: 取消知识导入任务
      parameters: [{ $ref: "#/components/parameters/Id" }]
      responses:
        "200": { description: 已取消或已请求 Worker 取消 }
  /knowledge/imports/{id}/structure:
    get:
      summary: 分页获取预检后的知识树摘要
      parameters:
        - { $ref: "#/components/parameters/Id" }
        - in: query
          name: cursor
          schema: { type: integer, minimum: 0, default: 0 }
        - in: query
          name: pageSize
          schema: { type: integer, minimum: 1, maximum: 200, default: 100 }
      responses:
        "200":
          description: 节点摘要和图片引用
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeImportStructure" }
  /knowledge/imports/{id}/issues:
    get:
      summary: 分页获取知识包预检问题
      parameters:
        - { $ref: "#/components/parameters/Id" }
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PageSize" }
      responses:
        "200":
          description: 预检问题分页
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeImportIssuePage" }
  /knowledge/imports/{id}/confirm:
    post:
      summary: 确认预检结果、原子预留容量并进入索引队列
      parameters: [{ $ref: "#/components/parameters/Id" }]
      responses:
        "200":
          description: 已确认
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeImportJob" }
  /knowledge/imports/{id}/retry-compensation:
    post:
      summary: 显式重试补偿失败任务的精确对象清理
      parameters: [{ $ref: "#/components/parameters/Id" }]
      responses:
        "200":
          description: 已重新进入补偿清理队列
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeImportJob" }
        "400": { $ref: "#/components/responses/ValidationError" }
  /knowledge/libraries/{libraryId}/documents:
    get:
      summary: 获取可访问知识库的文档
      parameters:
        - in: path
          name: libraryId
          required: true
          schema: { type: string }
      responses:
        "200":
          description: 文档列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeLibraryDocumentList" }
  /knowledge/documents/{documentId}/versions:
    get:
      summary: 获取文档版本列表
      parameters:
        - in: path
          name: documentId
          required: true
          schema: { type: string }
      responses:
        "200":
          description: 版本列表与活动版本
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeVersionList" }
  /knowledge/documents/{documentId}/versions/{versionId}/preview:
    get:
      summary: 管理者或私有 owner 获取单一候选文档预览清单
      description: 该接口只描述指定候选版本，不拼接知识库内其他文档；H1 仅在此管理预览中作为版本标识返回。
      parameters:
        - in: path
          name: documentId
          required: true
          schema: { type: string }
        - in: path
          name: versionId
          required: true
          schema: { type: string }
      responses:
        "200":
          description: 单候选预览清单
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgePreviewManifest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /knowledge/documents/{documentId}/versions/{versionId}/preview/range:
    get:
      summary: 双向获取单一候选文档的有界预览范围
      parameters:
        - in: path
          name: documentId
          required: true
          schema: { type: string }
        - in: path
          name: versionId
          required: true
          schema: { type: string }
        - in: query
          name: revision
          required: true
          schema: { type: string, minLength: 10, maxLength: 100 }
        - in: query
          name: anchor
          schema: { type: string, minLength: 1, maxLength: 191 }
        - in: query
          name: before
          schema: { type: integer, minimum: 0, maximum: 40, default: 8 }
        - in: query
          name: after
          schema: { type: integer, minimum: 0, maximum: 40, default: 16 }
      responses:
        "200":
          description: 仅来自指定候选版本的预览范围
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeReaderRangeResponse" }
        "409": { $ref: "#/components/responses/Conflict" }
  /knowledge/documents/{documentId}:
    delete:
      summary: 软删除知识文档并排队清理全部版本向量和图片
      parameters:
        - in: path
          name: documentId
          required: true
          schema: { type: string }
      responses:
        "200":
          description: 文档已失效并返回 cleanupPending
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeCleanupPending" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /knowledge/documents/{documentId}/tree:
    get:
      summary: 获取活动或指定版本的知识树
      parameters:
        - in: path
          name: documentId
          required: true
          schema: { type: string }
        - in: query
          name: versionId
          schema: { type: string }
      responses:
        "200":
          description: 知识树节点
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeTree" }
  /knowledge/documents/{documentId}/versions/{versionId}/publish:
    post:
      summary: 原子切换 Markdown 活动版本并同步 Qdrant payload
      parameters:
        - in: path
          name: documentId
          required: true
          schema: { type: string }
        - in: path
          name: versionId
          required: true
          schema: { type: string }
      responses:
        "200": { description: 版本已发布 }
        "503": { description: Qdrant 暂时不可用 }
  /knowledge/documents/{documentId}/rebuild:
    post:
      summary: 从活动 Markdown 创建不可变重建候选版本
      parameters:
        - in: path
          name: documentId
          required: true
          schema: { type: string }
      responses:
        "201": { description: 已创建重建任务 }
  /knowledge/documents/{documentId}/versions/{versionId}:
    delete:
      summary: 删除非活动版本并排队精确清理向量和图片关系
      parameters:
        - in: path
          name: documentId
          required: true
          schema: { type: string }
        - in: path
          name: versionId
          required: true
          schema: { type: string }
      responses:
        "200": { description: 清理任务已创建 }
  /knowledge/cleanup-jobs/{jobId}/retry:
    post:
      summary: 受权重试失败的知识向量或图片清理任务
      parameters:
        - in: path
          name: jobId
          required: true
          schema: { type: string }
      responses:
        "200":
          description: 清理任务已重新排队
          content:
            application/json:
              schema:
                type: object
                required: [jobId, queued]
                properties:
                  jobId: { type: string }
                  queued: { type: boolean, const: true }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /knowledge/manage:
    get:
      summary: 获取知识资料管理列表
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: query
          name: subjectId
          schema: { type: string }
      responses:
        "200":
          description: 包含草稿、索引和发布状态的管理列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeManageResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /knowledge/{id}:
    get:
      summary: 获取已发布资料详情
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        "200":
          description: 资料详情
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeDocumentDetail" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: 删除知识资料、检索分片和原始上传对象
      description: 数据库先执行软删除；对象存储清理失败时保留对象键并返回 storageDeleted=false，允许后续重试。
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        "200":
          description: 资料已从知识库删除
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KnowledgeDeleteResult" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /knowledge/articles:
    post:
      summary: 已移除的文章创建入口
      deprecated: true
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, body]
              properties:
                title: { type: string, minLength: 2, maxLength: 200 }
                body: { type: string, minLength: 20, maxLength: 500000 }
                subjectId: { type: string }
      responses:
        "410": { description: 新知识只接受 Markdown 或 ZIP 知识包 }
        "403": { $ref: "#/components/responses/Forbidden" }
  /knowledge/files:
    post:
      summary: 已移除的 PDF/DOCX 创建入口
      deprecated: true
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file: { type: string, format: binary }
                title: { type: string, maxLength: 200 }
                subjectId: { type: string }
      responses:
        "410": { description: 新知识只接受 Markdown 或 ZIP 知识包 }
        "413": { description: 文件超过 200 MiB 限制 }
        "403": { $ref: "#/components/responses/Forbidden" }
  /knowledge/{id}/publish:
    patch:
      summary: 发布已完成索引的资料
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        "200": { description: 已发布 }
        "400": { description: 资料尚未完成索引 }
  /knowledge/{id}/reindex:
    patch:
      summary: 重新加入资料索引队列
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        "200": { description: 已加入索引队列 }
  /quizzes/start:
    post:
      summary: 随机抽题或按题目 ID 创建练习快照
      description: 随机抽题默认排除往年真题；传 questionIds 时按给定顺序自由组卷。
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuizStartRequest" }
      responses:
        "201":
          description: 已生成不含答案的答题快照
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizStartResponse" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /quizzes/attempts/active:
    get:
      summary: 获取当前成员最多五个未过期答题草稿或评分任务
      responses:
        "200":
          description: 按最近保存时间倒序返回的可恢复答题
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizActiveAttemptList" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /quizzes/attempts/{attemptId}:
    get:
      summary: 仅由所有者读取答题草稿、评分状态或既有结果
      parameters:
        - in: path
          name: attemptId
          required: true
          schema: { type: string }
      responses:
        "200":
          description: 当前答题生命周期快照
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizAttemptLifecycle" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /quizzes/attempts/{attemptId}/draft:
    patch:
      summary: 按 revision 保存答案和位置并将草稿有效期续至七天后
      parameters:
        - in: path
          name: attemptId
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuizDraftSaveRequest" }
      responses:
        "200":
          description: 草稿已保存并递增 revision
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizDraftSaveResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /quizzes/attempts/{attemptId}/abandon:
    post:
      summary: 幂等放弃未提交答题；评分中或已提交答题拒绝放弃
      parameters:
        - in: path
          name: attemptId
          required: true
          schema: { type: string }
      responses:
        "201":
          description: 答题已放弃或此前已放弃
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizAbandonResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /quizzes/filters:
    get:
      summary: 获取按学科和章节分组的题型及往年真题筛选项
      responses:
        "200":
          description: 题库筛选树
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizFiltersResponse" }
  /quizzes/questions:
    get:
      summary: 分页浏览整个题库
      description: 返回题干、选项和配图，但不返回答案、解析或简答题评分标准。
      parameters:
        - in: query
          name: subjectId
          schema: { type: string }
        - in: query
          name: chapterIds
          style: form
          explode: false
          schema:
            type: array
            minItems: 1
            maxItems: 20
            items: { type: string }
        - in: query
          name: chapterMatch
          schema: { $ref: "#/components/schemas/ChapterMatch" }
        - in: query
          name: includeCrossChapter
          schema: { type: boolean }
        - in: query
          name: typeLabel
          schema: { type: string, maxLength: 100 }
        - in: query
          name: source
          schema: { $ref: "#/components/schemas/QuizQuestionSourceFilter" }
        - in: query
          name: pastPaper
          schema: { type: string, enum: [ALL, EXCLUDE, ONLY], default: ALL }
        - in: query
          name: paperId
          schema: { type: string }
        - in: query
          name: search
          schema: { type: string, maxLength: 200 }
        - in: query
          name: page
          schema: { type: integer, minimum: 1, default: 1 }
        - in: query
          name: pageSize
          schema: { type: integer, minimum: 1, maximum: 100, default: 30 }
      responses:
        "200":
          description: 无答案题目分页
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizLibraryResponse" }
    post:
      summary: 创建一道题目
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuizQuestionWriteRequest" }
      responses:
        "201": { description: 已创建 }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /quizzes/imports:
    get:
      summary: 分页查看题目导入任务
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: query
          name: page
          schema: { type: integer, minimum: 1, default: 1 }
        - in: query
          name: pageSize
          schema: { type: integer, minimum: 1, maximum: 50, default: 10 }
      responses:
        "200":
          description: 导入任务分页
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizImportListResponse" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: 上传 CSV 或 ZIP 并创建异步预检任务
      description: CSV 最大 2 MiB；ZIP 最大 200 MiB。任务预检通过后必须单独确认，上传本身不会创建题目、章节、试卷或图片。
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, mode]
              properties:
                file: { type: string, format: binary }
                mode: { $ref: "#/components/schemas/QuizImportMode" }
                createMissingChapters:
                  type: boolean
                  default: false
                  description: 确认导入时创建 CSV 中不存在的章节；停用章节仍会拒绝
                subjectId: { type: string }
                pastPaperId: { type: string }
                pastPaperTitle: { type: string, maxLength: 160 }
                pastPaperYear: { type: integer, minimum: 1900, maximum: 2200 }
      responses:
        "202":
          description: 已进入预检队列
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizImportJob" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /quizzes/imports/orphans:
    get:
      summary: 只读查看题目导入补偿失败对象
      x-required-roles: [ADMIN]
      responses:
        "200": { description: 导入孤儿对象报告 }
        "403": { $ref: "#/components/responses/Forbidden" }
  /quizzes/imports/{id}:
    get:
      summary: 获取导入任务、预检摘要与前 100 条问题
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 导入任务详情
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizImportJobDetail" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /quizzes/imports/{id}/confirm:
    post:
      summary: 幂等确认预检通过的导入任务
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201":
          description: 已确认或返回既有处理结果
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizImportJobDetail" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /quizzes/imports/{id}/cancel:
    post:
      summary: 幂等取消题库导入；已确认任务由 Worker 补偿并释放容量预留
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201":
          description: 已立即取消或记录异步取消请求
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizImportJobDetail" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /quizzes/imports/{id}/issues.csv:
    get:
      summary: 下载完整预检问题清单
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: UTF-8 CSV 问题清单
          content:
            text/csv:
              schema: { type: string }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /quizzes/questions/{id}:
    get:
      summary: 获取包含答案与评分标准的题目编辑数据
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 题目编辑数据
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizQuestionEditorData" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: 更新题目及其多章节关联
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuizQuestionWriteRequest" }
      responses:
        "200":
          description: 题目已更新
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizQuestionEditorData" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }
    delete:
      summary: 停用题目并保留历史答题快照
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 题目已停用；重复调用保持幂等成功
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizQuestionDeleteResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /quizzes/questions/{id}/images:
    post:
      summary: 上传并关联一道题目的配图
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file: { type: string, format: binary }
                caption: { type: string, maxLength: 300 }
                sortOrder: { type: integer, minimum: 0, maximum: 10000 }
      responses:
        "201":
          description: 配图已关联
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizImage" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /quizzes/questions/{questionId}/images/{photoId}:
    delete:
      summary: 解除题目与配图的关联
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: questionId
          required: true
          schema: { type: string }
        - in: path
          name: photoId
          required: true
          schema: { type: string }
      responses:
        "200": { description: 已解除关联 }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /quizzes/papers:
    get:
      summary: 获取往年真题试卷列表
      parameters:
        - in: query
          name: subjectId
          schema: { type: string }
      responses:
        "200":
          description: 往年真题列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizPaperListResponse" }
  /quizzes/history:
    get:
      summary: 获取当前成员已提交的答题历史
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: 按创建时间倒序返回的答题历史
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizHistoryResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /quizzes/wrong:
    get:
      summary: 查询当前成员的错题索引
      description: 每道错题保留累计答错次数、最近错误时间、最近题目快照及简答题最近错误反馈；答对不会删除历史错题。
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
        - in: query
          name: subjectId
          schema: { type: string }
        - in: query
          name: chapterIds
          description: 逗号分隔的章节 ID，最多 20 个
          schema: { type: string }
        - in: query
          name: chapterMatch
          schema: { $ref: "#/components/schemas/ChapterMatch" }
        - in: query
          name: includeCrossChapter
          schema: { type: boolean, default: false }
        - in: query
          name: typeLabel
          schema: { type: string, maxLength: 100 }
        - in: query
          name: source
          schema: { $ref: "#/components/schemas/QuizQuestionSourceFilter" }
        - in: query
          name: search
          schema: { type: string, maxLength: 200 }
      responses:
        "200":
          description: 最近错题列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizWrongResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /quizzes/papers/{paperId}/start:
    post:
      summary: 按试卷顺序开始整套往年真题测试
      parameters:
        - in: path
          name: paperId
          required: true
          schema: { type: string }
      responses:
        "201":
          description: 已生成整套试卷答题快照
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizStartResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
  /quizzes/{attemptId}/submit:
    post:
      summary: 按不可变快照和规范化答案幂等提交并自动判分
      parameters:
        - in: path
          name: attemptId
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuizSubmitRequest" }
      responses:
        "200":
          description: 已完成评分；相同 submissionHash 重复提交返回同一结果
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizSubmitCompletedResponse" }
        "202":
          description: 相同答案正在由持有租约的请求评分
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizSubmitPendingResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /daily-practice/today:
    get:
      summary: 获取当前用户在 Asia/Shanghai 04:00 日界线下的今日练习
      responses:
        "200":
          description: 今日状态、计划、总结、建议配额及权威日界线
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeToday" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /daily-practice/plans/{planId}/start:
    post:
      summary: 幂等开始当前有效的每日练习计划
      parameters:
        - in: path
          name: planId
          required: true
          schema: { type: string }
      responses:
        "201":
          description: 已创建或返回同一答题快照
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeStartResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /daily-practice/suggestions:
    post:
      summary: 为下一练习日提交一次个性化建议
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DailyPracticeSuggestionRequest" }
      responses:
        "201":
          description: 建议已保存，下一练习日自动计划消费
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeSuggestion" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/Conflict" }
  /daily-practice/history:
    get:
      summary: 分页获取当前用户的每日练习历史
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: 每日练习历史
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeHistoryPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /daily-practice/history/{planId}:
    get:
      summary: 获取当前用户的一次每日练习详情
      parameters:
        - in: path
          name: planId
          required: true
          schema: { type: string }
      responses:
        "200":
          description: 历史计划、题目与有限成绩摘要
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeHistoryDetail" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/daily-practice/settings:
    get:
      summary: 获取每日一练总开关及当前有效服务状态
      x-required-roles: [EDITOR, ADMIN]
      responses:
        "200":
          description: 每日一练服务配置
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeSettings" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    patch:
      summary: 按修订号开启或关闭每日一练
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DailyPracticeSettingsUpdateRequest" }
      responses:
        "200":
          description: 更新后的服务配置
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeSettings" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/daily-practice/service-pauses:
    get:
      summary: 分页获取假期停服与取消记录
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: 停服时段列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeServicePausePage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: 创建立即或未来生效的停服时段
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DailyPracticeServicePauseCreateRequest" }
      responses:
        "201":
          description: 停服时段已创建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeServicePause" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/daily-practice/service-pauses/{id}/cancel:
    post:
      summary: 提前恢复或取消未来停服时段
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201":
          description: 停服记录已标记取消
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeServicePause" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/daily-practice/teaching-progress:
    get:
      summary: 分页获取不可变教学进度版本
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
        - in: query
          name: subjectId
          schema: { type: string }
      responses:
        "200":
          description: 教学进度版本列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TeachingProgressPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: 发布教学进度完整快照（最早可从当日练习日生效）
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TeachingProgressPublishRequest" }
      responses:
        "201":
          description: 新教学进度版本
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TeachingProgressDetail" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/daily-practice/teaching-progress/{id}:
    get:
      summary: 获取教学进度版本及未解析节点报告
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 教学进度详情
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TeachingProgressDetail" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /admin/daily-practice/fixed-question-candidates:
    get:
      summary: 分页筛选可作为全班附加题的 MANUAL/CSV 题目
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
        - in: query
          name: subjectId
          schema: { type: string }
        - in: query
          name: chapterIds
          schema: { type: string, description: 逗号分隔章节 ID }
        - in: query
          name: chapterMatch
          schema: { type: string, enum: [ANY, ALL], default: ANY }
        - in: query
          name: includeCrossChapter
          schema: { type: boolean, default: false }
        - in: query
          name: gradingType
          schema: { $ref: "#/components/schemas/QuestionType" }
        - in: query
          name: typeLabel
          schema: { type: string, maxLength: 100 }
        - in: query
          name: pastPaper
          schema: { type: string, enum: [ALL, EXCLUDE, ONLY] }
        - in: query
          name: paperId
          schema: { type: string }
        - in: query
          name: search
          schema: { type: string, maxLength: 200 }
      responses:
        "200":
          description: 非 AI 固定题候选
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeFixedQuestionPage" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /admin/daily-practice/fixed-assignments/{practiceDate}:
    get:
      summary: 获取某练习日最新固定附加题修订
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: practiceDate
          required: true
          schema: { type: string, format: date }
      responses:
        "200":
          description: 最新修订；从未发布时 assignment 为 null 且 revision 为 0
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeFixedAssignmentEnvelope" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /admin/daily-practice/fixed-assignments:
    post:
      summary: 发布未来练习日的 0-20 道全班固定附加题
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DailyPracticeFixedAssignmentPublishRequest" }
      responses:
        "201":
          description: 固定题修订已发布
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeFixedAssignment" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/daily-practice/cycles:
    get:
      summary: 分页获取每日练习周期摘要（默认近 14 个练习日）
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
        - in: query
          name: from
          schema: { type: string, format: date }
        - in: query
          name: to
          schema: { type: string, format: date }
      responses:
        "200":
          description: 周期摘要列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeCyclePage" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /admin/daily-practice/cycles/{practiceDate}:
    get:
      summary: 获取不含用户身份的每日周期聚合运行状态
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: practiceDate
          required: true
          schema: { type: string, format: date }
      responses:
        "200":
          description: 匿名周期聚合
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeCycleAggregate" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/daily-practice/cycles/{practiceDate}/refreeze:
    post:
      summary: 请求按当前教学进度与题库状态重新冻结当前练习日周期并重建未开始计划（Worker 异步执行）
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: practiceDate
          required: true
          schema: { type: string, format: date }
      responses:
        "201":
          description: 重新冻结已排队；alreadyRequested 表示此前已有未执行的请求
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeCycleRefreezeResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/daily-practice/users:
    get:
      summary: 分页获取用户每日个性化状态
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
        - in: query
          name: search
          schema: { type: string, maxLength: 80 }
        - in: query
          name: role
          schema: { $ref: "#/components/schemas/Role" }
        - in: query
          name: status
          schema: { $ref: "#/components/schemas/AccountStatus" }
        - in: query
          name: initializationStatus
          schema: { $ref: "#/components/schemas/UserPracticeInitializationStatus" }
        - in: query
          name: planStatus
          schema: { $ref: "#/components/schemas/DailyPracticeDayStatus" }
        - in: query
          name: generationSource
          schema: { $ref: "#/components/schemas/DailyPracticeGenerationSource" }
        - in: query
          name: suggestionStatus
          schema: { $ref: "#/components/schemas/DailyPracticeSuggestionStatus" }
        - in: query
          name: completed
          schema: { type: boolean }
        - in: query
          name: hasSuggestion
          schema: { type: boolean }
        - in: query
          name: hasGap
          schema: { type: boolean }
      responses:
        "200":
          description: ADMIN-only 用户状态页
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminDailyPracticeUserPage" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /admin/daily-practice/users/{userId}:
    get:
      summary: 获取单个用户的画像、计划修订和建议审计
      x-required-roles: [ADMIN]
      parameters:
        - in: path
          name: userId
          required: true
          schema: { type: string }
      responses:
        "200":
          description: ADMIN-only 用户个性化详情
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminDailyPracticeUserDetail" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /admin/daily-practice/users/{userId}/suggestions:
    post:
      summary: 为指定用户提交不限制业务次数的管理员建议
      x-required-roles: [ADMIN]
      parameters:
        - in: path
          name: userId
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminDailyPracticeSuggestionRequest" }
      responses:
        "201":
          description: 管理员建议已保存并保留审计
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DailyPracticeSuggestion" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /admin/daily-practice/users/{userId}/plans/{practiceDate}/regenerate:
    post:
      summary: 为指定用户排入新的发布型计划修订
      x-required-roles: [ADMIN]
      parameters:
        - in: path
          name: userId
          required: true
          schema: { type: string }
        - in: path
          name: practiceDate
          required: true
          schema: { type: string, format: date }
      responses:
        "201":
          description: ADMIN_REGENERATE 修订已排队
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminDailyPracticePlanAction" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/daily-practice/users/{userId}/plans/{practiceDate}/preview:
    post:
      summary: 为指定用户排入不发布的诊断预览修订
      x-required-roles: [ADMIN]
      parameters:
        - in: path
          name: userId
          required: true
          schema: { type: string }
        - in: path
          name: practiceDate
          required: true
          schema: { type: string, format: date }
      responses:
        "201":
          description: ADMIN_PREVIEW 修订已排队
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminDailyPracticePlanAction" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /ai/question-generation/sources/libraries:
    get:
      summary: 列出同学科可用于 AI 出题的共享发布知识库
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: query
          name: subjectId
          required: true
          schema: { type: string }
      responses:
        "200": { description: 可用共享知识库 }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /ai/question-generation/sources/libraries/{libraryId}/nodes:
    get:
      summary: 分页列出已发布知识标题及其末级证据选择元数据
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: libraryId
          required: true
          schema: { type: string }
        - in: query
          name: subjectId
          required: true
          schema: { type: string }
        - in: query
          name: query
          schema: { type: string, maxLength: 200 }
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PageSize" }
      responses:
        "200": { description: 可选知识节点 }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /ai/question-generation-jobs/preview:
    post:
      summary: 在不调用模型的前提下预检出题范围
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiQuestionGenerationRequest" }
      responses:
        "201":
          description: 预检摘要
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AiQuestionGenerationPreview" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /ai/question-generation-jobs:
    get:
      summary: 分页列出 AI 出题任务
      x-required-roles: [EDITOR, ADMIN]
      responses:
        "200":
          description: 出题任务列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AiQuestionGenerationJobPage" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: 幂等创建 AI 出题任务
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: header
          name: Idempotency-Key
          required: true
          schema: { type: string, minLength: 1, maxLength: 191 }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiQuestionGenerationRequest" }
      responses:
        "201":
          description: 已创建或返回同一幂等任务
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AiQuestionGenerationJob" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /ai/question-generation-jobs/{id}:
    get:
      summary: 获取 AI 出题任务详情和非正文调用审计
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - { $ref: "#/components/parameters/Id" }
      responses:
        "200":
          description: 出题任务详情
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AiQuestionGenerationJob" }
        "404": { $ref: "#/components/responses/NotFound" }
  /ai/question-generation-jobs/{id}/cancel:
    post:
      summary: 取消自己创建的排队或运行中任务；管理员可取消任意任务
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - { $ref: "#/components/parameters/Id" }
      responses:
        "201": { description: 已接受取消 }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /ai/question-reviews:
    get:
      summary: 分页列出 AI 题目审核队列
      x-required-roles: [EDITOR, ADMIN]
      responses:
        "200":
          description: 审核队列
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AiQuestionReviewPage" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /ai/question-reviews/{questionId}:
    get:
      summary: 获取 AI 题当前稿、原始生成稿、精确证据和审核事件
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: questionId
          required: true
          schema: { type: string }
      responses:
        "200":
          description: 审核详情
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AiQuestionReviewDetail" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: 按审核版本保存 AI 题草稿
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: questionId
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiQuestionReviewWriteRequest" }
      responses:
        "200": { description: 已保存草稿 }
        "409": { $ref: "#/components/responses/Conflict" }
  /ai/question-reviews/{questionId}/approve:
    post:
      summary: 原子保存并发布单道 AI 题
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: questionId
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiQuestionReviewWriteRequest" }
      responses:
        "201": { description: 已发布题目 }
        "409": { $ref: "#/components/responses/Conflict" }
  /ai/question-reviews/{questionId}/reject:
    post:
      summary: 按审核版本拒绝单道 AI 题
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: questionId
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiQuestionRejectRequest" }
      responses:
        "201": { description: 已拒绝题目 }
        "409": { $ref: "#/components/responses/Conflict" }
  /ai/question-reviews/{questionId}/reopen:
    post:
      summary: 按审核 revision 将已批准 AI 题显式撤回至待审核草稿
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: questionId
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiQuestionReopenRequest" }
      responses:
        "201":
          description: 已撤回；新练习与每日计划不再选择该题
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AiQuestionReviewDetail" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /ai/question-reviews/revalidate-source/bulk:
    post:
      summary: 批量自动重绑待复核 AI 题的当前知识来源
      x-required-roles: [EDITOR, ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiQuestionSourceBulkRevalidateRequest" }
      responses:
        "201":
          description: 逐题重绑结果；单题失败不影响其他题目
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AiQuestionSourceBulkRevalidateResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
  /ai/question-reviews/{questionId}/revalidate-source:
    post:
      summary: 将 AI 题重新绑定到当前有效的显式知识节点
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - in: path
          name: questionId
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiQuestionSourceRevalidateRequest" }
      responses:
        "201": { description: 来源已复核 }
        "409": { $ref: "#/components/responses/Conflict" }
  /ai/conversations:
    get:
      summary: 分页列出当前用户的 AI 问答会话
      description: 仅返回当前用户拥有的会话，按 updatedAt 与 id 组成的稳定游标倒序分页。
      parameters:
        - in: query
          name: cursor
          schema: { type: string, maxLength: 512 }
        - in: query
          name: pageSize
          schema: { type: integer, minimum: 1, maximum: 50, default: 20 }
      responses:
        "200":
          description: 当前用户的会话摘要页
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AiConversationListResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /ai/conversations/{id}:
    get:
      summary: 获取当前用户拥有的 AI 问答会话
      description: 返回最近 200 条消息，并通过 hasEarlierMessages 标记是否存在更早消息。
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 会话详情
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AiConversationDetail" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: 显式删除当前用户拥有的 AI 问答会话
      description: 会话不会自动过期；删除会话和写入审计日志位于同一事务。
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 会话已删除
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AiConversationDeleteResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /ai/chat:
    post:
      summary: 基于固定知识范围、Qdrant 召回和 MySQL 二次授权的流式问答
      description: 新会话固定学科、模式、1-20 个库和可选章节；续聊只能提交 conversationId。事件顺序为 meta、token、done，失败为 error；assistant 仅在完整生成后持久化。
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KnowledgeChatRequest" }
      responses:
        "200":
          description: meta/token/done 或稳定 error 事件的数据流；不返回 reasoning_content
          content:
            text/event-stream:
              schema: { type: string }
              examples:
                meta:
                  value: "event: meta\ndata: {\"conversationId\":\"conversation-id\",\"citations\":[],\"images\":[]}\n\n"
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "503": { description: Embedding 或 Qdrant 当前不可用，不回退关键词检索 }

  /credit-hours/submissions:
    get:
      summary: 分页读取当前用户的完整学时提交
      responses:
        "200": { description: 当前用户学时记录页 }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: 以半小时精度提交一条学时记录和 1-5 张凭证
      parameters:
        - in: header
          name: Idempotency-Key
          required: true
          schema: { type: string, minLength: 1, maxLength: 191 }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              additionalProperties: false
              required: [type, activityName, hours, sourceDescription, evidence]
              properties:
                type: { type: string, enum: [QUALITY, VOLUNTEER] }
                activityName: { type: string, minLength: 1, maxLength: 160 }
                hours: { type: number, minimum: 0.5, maximum: 1000, multipleOf: 0.5 }
                sourceDescription: { type: string, minLength: 1, maxLength: 2000 }
                replacesSubmissionId: { type: string }
                evidence:
                  type: array
                  minItems: 1
                  maxItems: 5
                  items: { type: string, format: binary }
      responses:
        "201": { description: 提交与视觉审核任务已创建 }
        "409": { $ref: "#/components/responses/Conflict" }
        "413": { description: 图片或总请求体过大 }
        "422": { $ref: "#/components/responses/ValidationError" }
  /credit-hours/submissions/{id}:
    get:
      summary: 读取当前用户的一条完整学时记录
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 包含来源、决定理由、人工审核提示和展示图入口的本人详情
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CreditHourSubmission" }
        "404": { $ref: "#/components/responses/NotFound" }
  /credit-hours/submissions/{id}/withdraw:
    post:
      summary: 携带 expectedRevision 撤回待审核记录
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201": { description: 记录已撤回 }
        "409": { $ref: "#/components/responses/Conflict" }
  /credit-hours/evidence/{id}/display:
    get:
      summary: 当前 owner 或 ADMIN 读取学时展示图
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200": { description: database 模式的私有图片字节 }
        "302": { description: COS 模式的短时私有签名跳转 }
        "403": { $ref: "#/components/responses/Forbidden" }
  /credit-hours/me/summary:
    get:
      summary: 获取当前用户两类及合计的已通过小时数与状态计数
      responses:
        "200": { description: 本人学时汇总，小时最多一位小数 }
  /credit-hours/leaderboard:
    get:
      summary: 获取全部 ACTIVE 用户的两类学时、总学时和所选维度公示排行
      description: TOTAL 仅是 QUALITY 与 VOLUNTEER 的只读合计维度，不能用于提交；只聚合已通过且未删除记录，同学时共享名次，不返回账号、学号、来源或凭证。
      parameters:
        - in: query
          name: type
          schema: { type: string, enum: [QUALITY, VOLUNTEER, TOTAL], default: QUALITY }
        - in: query
          name: cursor
          schema: { type: string }
        - in: query
          name: pageSize
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200": { description: 全员公示排行 }
  /credit-hours/users/{userId}/submissions:
    get:
      summary: 获取指定 ACTIVE 用户的已通过公示记录
      description: 只返回活动名称、类型、小时数和通过时间。
      parameters:
        - in: path
          name: userId
          required: true
          schema: { type: string }
      responses:
        "200": { description: 他人已通过记录页 }
        "404": { $ref: "#/components/responses/NotFound" }
  /admin/credit-hours/submissions:
    get:
      summary: 管理员筛选学时审核队列和完整记录
      x-required-roles: [ADMIN]
      responses:
        "200": { description: 管理记录页 }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: 管理员为其他正常状态用户直接录入一条已通过学时记录
      description: 不接收凭证、不创建 AI 审核任务；写入 ADMIN_CREATED 决策来源、决定事件和审计日志。目标用户不能是当前管理员本人。
      x-required-roles: [ADMIN]
      parameters:
        - in: header
          name: Idempotency-Key
          required: true
          schema: { type: string, minLength: 1, maxLength: 191 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [userId, type, activityName, hours, description]
              properties:
                userId: { type: string, minLength: 1, maxLength: 191 }
                type: { type: string, enum: [QUALITY, VOLUNTEER] }
                activityName: { type: string, minLength: 1, maxLength: 160 }
                hours: { type: number, minimum: 0.5, maximum: 1000, multipleOf: 0.5 }
                description: { type: string, minLength: 1, maxLength: 2000 }
      responses:
        "201": { description: 已创建直接通过且无凭证、无审核任务的记录 }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /admin/credit-hours/submissions/{id}:
    get:
      summary: 管理员读取审核轮次、尝试和事件详情
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200": { description: 管理详情，不含原图字节、对象 key 或提供商文件 ID }
  /admin/credit-hours/evidence/{id}/original:
    get:
      summary: 管理员按需读取并审计一张原始凭证
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200": { description: database 模式的原图字节 }
        "302": { description: COS 模式的短时私有签名跳转 }
  /admin/credit-hours/overview:
    get:
      summary: 管理员按总学时从低到高查看全部 ACTIVE 用户
      description: 固定按 QUALITY 与 VOLUNTEER 的已通过且未删除记录合计排序，包含零学时用户，不接受学时类型筛选。
      x-required-roles: [ADMIN]
      responses:
        "200": { description: 管理总览 }
  /admin/credit-hours/exports/approved.zip:
    get:
      summary: 流式导出总学时榜单、全部已通过记录及当前修订原始凭证
      description: ZIP 包含 total-leaderboard.csv、approved-records.csv 和 evidence/{submissionId}/{evidenceId}.{ext}；不包含学号、哈希、对象 key、提供商文件 ID 或 AI 原始响应，并使用 private, no-store。
      x-required-roles: [ADMIN]
      responses:
        "200":
          description: UTF-8 BOM CSV 与逐张校验原始凭证组成的流式 ZIP；凭证读取或校验失败时中断响应，使归档无法被当作完整 ZIP 使用
          content:
            application/zip:
              schema: { type: string, format: binary }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/credit-hours/runtime:
    get:
      summary: 获取审核任务、临时文件清理和删除操作计数
      x-required-roles: [ADMIN]
      responses:
        "200": { description: 不包含文件 ID 或对象 key 的运行摘要 }
  /admin/credit-hours/operations/{id}:
    get:
      summary: 查看单条学时凭证清理操作状态
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200": { description: 不包含对象 key 的清理进度 }
        "404": { $ref: "#/components/responses/NotFound" }
  /admin/credit-hours/operations/{id}/resume:
    post:
      summary: 幂等续跑部分失败或失败的单条凭证清理
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201": { description: 返回 COMPLETE、PARTIAL 或 FAILED 清理状态 }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/credit-hours/submissions/{id}/decisions:
    post:
      summary: 管理员处理待人工审核或覆盖最终通过和不通过决定
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201": { description: 决定已覆盖并审计 }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/credit-hours/submissions/{id}/reopen:
    post:
      summary: 管理员重新打开记录并创建新的视觉审核轮次
      description: 无凭证的管理员直接录入记录及待人工审核记录不能重新打开进入 AI 审核。
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201": { description: 新审核轮次已创建 }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/credit-hours/submissions/{id}/ai-retry:
    post:
      summary: 管理员受控重试当前失败的视觉审核任务
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201": { description: 任务已重新排队 }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/credit-hours/submissions/{id}/deletion:
    post:
      summary: 管理员逻辑删除记录并按精确清单清理凭证
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201": { description: 返回 COMPLETE、PARTIAL 或 FAILED 清理状态 }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/credit-hours/initializations/preflight:
    post:
      summary: 只读统计全部初始化影响范围
      description: 不执行 root 命令、备份或清理；执行阶段仍要求独立运维授权。
      x-required-roles: [ADMIN]
      responses:
        "201": { description: 记录、任务、凭证和字节计数 }

  /albums/{id}/originals/summary:
    get:
      summary: 编辑或管理员读取相册原图数量与字节总数
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 历史照片缺失原图时只统计缺失，不从 WebP 重建
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AlbumOriginalSummary" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /albums/{id}/originals.zip:
    get:
      summary: 流式下载相册原图 ZIP
      description: private, no-store。包含 originals/ 下的原图及 manifest.jsonl；历史缺图标为 ORIGINAL_NOT_RETAINED，不暴露对象键。逐张校验大小及 SHA-256，读取失败或权限变化时中止。与学时导出共享单并发名额。
      x-required-roles: [EDITOR, ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 原图归档
          content:
            application/zip:
              schema: { type: string, format: binary }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { description: 无保留原图或已有导出正在生成 }
  /admin/credit-hours/submission-batches:
    post:
      summary: 管理员以相同活动和理由原子录入多名用户学时
      description: ADMIN 专用；1-100 个不同 ACTIVE 用户，禁止包含本人。每人独立小时数，整批成功或回滚；同键同内容返回原批次，变更内容返回 409。直接 APPROVED，不创建凭证或 AI 审核任务。
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminCreditHourBatchCreateRequest" }
      responses:
        "201":
          description: 已提交的完整批次（包括幂等重放）
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminCreditHourBatchCreateResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }
  /announcements/status:
    get:
      summary: 获取最新已发布且未撤回的公告及当前账号回执
      description: 先选最新再查回执；确认最新后不依次强制展示旧公告。所有公告响应 private, no-store。
      responses:
        "200":
          description: 无公告时 latest 为 null
          content:
            application/json:
              schema:
                type: object
                required: [latest]
                properties:
                  latest:
                    anyOf:
                      - $ref: "#/components/schemas/Announcement"
                      - { type: "null" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /announcements:
    get:
      summary: 分页读取已发布且未撤回公告的历史摘要
      parameters:
        - $ref: "#/components/parameters/AnnouncementCursor"
        - $ref: "#/components/parameters/AnnouncementPageSize"
      responses:
        "200":
          description: 按发布时间和 ID 倒序，读取不产生回执
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AnnouncementPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /announcements/{id}:
    get:
      summary: 读取公告正文，不自动确认已读
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200": { $ref: "#/components/responses/AnnouncementDetail" }
        "404": { $ref: "#/components/responses/NotFound" }
  /announcements/{id}/read:
    post:
      summary: 当前账号显式确认公告已读
      description: 通过 CSRF 校验后幂等写入账号回执；不接受客户端指定用户。已撤回公告不可确认。
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "201":
          description: 账号回执
          content:
            application/json:
              schema:
                type: object
                required: [id, readAt]
                properties:
                  id: { type: string }
                  readAt: { type: string, format: date-time }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /admin/announcements:
    get:
      summary: 管理员分页筛选公告摘要
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/AnnouncementCursor"
        - $ref: "#/components/parameters/AnnouncementPageSize"
        - in: query
          name: keyword
          schema: { type: string, minLength: 1, maxLength: 160 }
        - in: query
          name: status
          schema: { type: string, enum: [DRAFT, PUBLISHED, WITHDRAWN] }
      responses:
        "200":
          description: 按创建时间和 ID 倒序的管理列表
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AnnouncementPage" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: 管理员创建纯文本公告草稿
      x-required-roles: [ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AnnouncementDraft" }
      responses:
        "201": { $ref: "#/components/responses/AnnouncementDetail" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /admin/announcements/{id}:
    parameters:
      - $ref: "#/components/parameters/Id"
    get:
      summary: 管理员读取草稿、发布或撤回公告正文
      x-required-roles: [ADMIN]
      responses:
        "200": { $ref: "#/components/responses/AnnouncementDetail" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: 按版本更新草稿，发布后的内容不可修改
      x-required-roles: [ADMIN]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              allOf:
                - $ref: "#/components/schemas/AnnouncementDraft"
                - $ref: "#/components/schemas/AnnouncementAction"
      responses:
        "200": { $ref: "#/components/responses/AnnouncementDetail" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    delete:
      summary: 按版本删除草稿
      x-required-roles: [ADMIN]
      requestBody: { $ref: "#/components/requestBodies/AnnouncementAction" }
      responses:
        "200":
          description: 草稿已删除
          content:
            application/json:
              schema:
                type: object
                required: [id, deleted]
                properties:
                  id: { type: string }
                  deleted: { type: boolean, const: true }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/announcements/{id}/publish:
    post:
      summary: 按版本原子发布公告
      description: 发布时间由服务端确定；相同版本的成功发布可幂等重放。
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody: { $ref: "#/components/requestBodies/AnnouncementAction" }
      responses:
        "201": { $ref: "#/components/responses/AnnouncementDetail" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/announcements/{id}/withdraw:
    post:
      summary: 按版本撤回已发布公告，保留回执和审计
      x-required-roles: [ADMIN]
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody: { $ref: "#/components/requestBodies/AnnouncementAction" }
      responses:
        "201": { $ref: "#/components/responses/AnnouncementDetail" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

components:
  requestBodies:
    AnnouncementAction:
      required: true
      content:
        application/json:
          schema: { $ref: "#/components/schemas/AnnouncementAction" }
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: bmc3_session
  parameters:
    IdempotencyKey:
      in: header
      name: Idempotency-Key
      required: true
      schema: { type: string, minLength: 1, maxLength: 191 }
    AnnouncementCursor:
      in: query
      name: cursor
      schema: { type: string, minLength: 1, maxLength: 512 }
    AnnouncementPageSize:
      in: query
      name: pageSize
      schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
    Id:
      in: path
      name: id
      required: true
      schema: { type: string }
    Page:
      in: query
      name: page
      schema: { type: integer, minimum: 1, default: 1 }
    PageSize:
      in: query
      name: pageSize
      schema: { type: integer, minimum: 1, maximum: 100, default: 100 }
    IncludeNewsBody:
      in: query
      name: includeBody
      description: 兼容旧客户端的正文开关；新列表客户端应传 false。
      schema: { type: boolean, default: true }
  responses:
    AnnouncementDetail:
      description: 纯文本公告详情；成员响应含账号 readAt，读取不产生已读副作用
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Announcement" }
    Unauthorized:
      description: 未登录、会话失效或凭据无效
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiError" }
    Forbidden:
      description: 当前账号无权限或操作被安全规则拒绝
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiError" }
    NotFound:
      description: 目标不存在
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiError" }
    Conflict:
      description: 操作与当前资源状态冲突
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiError" }
    ValidationError:
      description: 请求体校验失败
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiError" }
  schemas:
    CreditHourSubmission:
      type: object
      required: [id, status, revision, hours, activityName, sourceDescription, evidence]
      properties:
        id: { type: string }
        status: { $ref: "#/components/schemas/CreditHourSubmissionStatus" }
        revision: { type: integer, minimum: 1 }
        hours: { type: number, minimum: 0.5, maximum: 1000, multipleOf: 0.5 }
        activityName: { type: string }
        sourceDescription: { type: string }
        evidence: { type: array, items: { type: object } }
        manualReview:
          anyOf:
            - $ref: "#/components/schemas/CreditHourManualReview"
            - { type: "null" }
    AlbumOriginalSummary:
      type: object
      required: [albumId, photoCount, originalCount, missingOriginalCount, originalBytes, canExport]
      properties:
        albumId: { type: string }
        photoCount: { type: integer, minimum: 0 }
        originalCount: { type: integer, minimum: 0 }
        missingOriginalCount: { type: integer, minimum: 0 }
        originalBytes: { type: integer, minimum: 0 }
        canExport: { type: boolean }
    AdminCreditHourBatchCreateRequest:
      type: object
      additionalProperties: false
      required: [type, activityName, description, entries]
      properties:
        type: { type: string, enum: [QUALITY, VOLUNTEER] }
        activityName: { type: string, minLength: 1, maxLength: 160 }
        description: { type: string, minLength: 1, maxLength: 2000 }
        entries:
          type: array
          minItems: 1
          maxItems: 100
          description: userId 必须唯一；目标为其他 ACTIVE 用户
          items:
            type: object
            additionalProperties: false
            required: [userId, hours]
            properties:
              userId: { type: string, minLength: 1, maxLength: 191 }
              hours: { type: number, minimum: 0.5, maximum: 1000, multipleOf: 0.5 }
    AdminCreditHourBatchCreateResponse:
      type: object
      required: [id, count, items]
      properties:
        id: { type: string }
        count: { type: integer, minimum: 1, maximum: 100 }
        items:
          type: array
          items:
            type: object
            required: [id, userId, hours]
            properties:
              id: { type: string }
              userId: { type: string }
              hours: { type: number, minimum: 0.5, maximum: 1000, multipleOf: 0.5 }
    CreditHourSubmissionStatus:
      type: string
      enum: [PENDING_REVIEW, PENDING_MANUAL_REVIEW, APPROVED, REJECTED, WITHDRAWN]
      description: PENDING_MANUAL_REVIEW 不计入已通过汇总，允许本人撤回；须由管理员作出通过或拒绝决定，不能以 AI 重试或重新打开绕过人工分流。
    CreditHourManualReview:
      type: object
      required: [transferredAt, reason, riskCodes]
      properties:
        transferredAt: { type: [string, "null"], format: date-time }
        reason: { type: string }
        riskCodes:
          type: array
          items:
            type: string
            enum: [OFFICIAL_SEAL_MISSING, OFFICIAL_SEAL_UNCLEAR, SUSPECTED_AI_GENERATION, SUSPECTED_IMAGE_MANIPULATION, EVIDENCE_AUTHENTICITY_UNCERTAIN]
    AnnouncementDraft:
      type: object
      required: [title, body]
      properties:
        title: { type: string, minLength: 1, maxLength: 160 }
        body: { type: string, minLength: 1, maxLength: 20000, description: 纯文本，保留换行，不解释 HTML }
    AnnouncementAction:
      type: object
      required: [expectedRevision]
      properties:
        expectedRevision: { type: integer, minimum: 1 }
    AnnouncementSummary:
      type: object
      required: [id, title, status, revision, publishedAt, withdrawnAt, createdAt, updatedAt]
      properties:
        id: { type: string }
        title: { type: string, maxLength: 160 }
        status: { type: string, enum: [DRAFT, PUBLISHED, WITHDRAWN] }
        revision: { type: integer, minimum: 1 }
        publishedAt: { type: [string, "null"], format: date-time }
        withdrawnAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        readAt: { type: [string, "null"], format: date-time }
    Announcement:
      allOf:
        - $ref: "#/components/schemas/AnnouncementSummary"
        - type: object
          required: [body]
          properties:
            body: { type: string, maxLength: 20000 }
    AnnouncementPage:
      type: object
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AnnouncementSummary" }
        nextCursor: { type: [string, "null"] }
    HealthResponse:
      type: object
      additionalProperties: false
      required: [status, time]
      properties:
        status: { type: string, const: ok }
        time: { type: string, format: date-time }
    ReadinessResponse:
      type: object
      additionalProperties: false
      required: [status, time, dependencies]
      properties:
        status: { type: string, const: ok }
        time: { type: string, format: date-time }
        dependencies:
          type: object
          additionalProperties: false
          required: [database, vector]
          properties:
            database: { type: string, const: ok }
            vector:
              type: object
              additionalProperties: false
              required: [collection, alias, status, dimensions]
              properties:
                collection: { type: string }
                alias: { type: string }
                status: { type: string }
                dimensions: { type: integer, minimum: 1 }
    Role:
      type: string
      enum: [MEMBER, EDITOR, ADMIN]
    AccountStatus:
      type: string
      enum: [PENDING, ACTIVE, SUSPENDED]
    Visibility:
      type: string
      enum: [PUBLIC, MEMBERS]
    ContentStatus:
      type: string
      enum: [DRAFT, PUBLISHED, ARCHIVED]
    NewsEditableStatus:
      type: string
      enum: [DRAFT, PUBLISHED]
    KnowledgeKind:
      type: string
      enum: [ARTICLE, PDF, DOCX, MARKDOWN]
    KnowledgeLibraryScope:
      type: string
      enum: [SHARED, PRIVATE]
    KnowledgeConversationMode:
      type: string
      enum: [SHARED, PRIVATE, COMBINED]
    KnowledgeRenderStatus:
      type: string
      enum: [PENDING, READY, FAILED]
    KnowledgeImportStatus:
      type: string
      enum: [PREFLIGHT_PENDING, PREFLIGHTING, INVALID, AWAITING_CONFIRMATION, INDEX_PENDING, PROCESSING, READY, FAILED, COMPENSATION_FAILED, EXPIRED]
    ImportSourceCleanupStatus:
      type: string
      enum: [RETAINED, PENDING, IN_PROGRESS, CLEANED, FAILED]
    IndexStatus:
      type: string
      enum: [PENDING, PROCESSING, READY, FAILED]
    ChapterMatch:
      type: string
      enum: [ANY, ALL]
      default: ANY
    QuizQuestionCategory:
      type: string
      enum: [STANDARD, KNOWLEDGE_RECALL]
    QuizQuestionOrigin:
      type: string
      enum: [MANUAL, CSV, AI_GENERATED]
    QuizQuestionSourceFilter:
      type: string
      enum: [AI, NON_AI]
    Subject:
      type: object
      additionalProperties: false
      required: [id, name, slug, sortOrder, active]
      properties:
        id: { type: string }
        name: { type: string, maxLength: 100 }
        slug: { type: string, maxLength: 100 }
        sortOrder: { type: integer }
        active: { type: boolean }
    SubjectChapter:
      type: object
      additionalProperties: false
      required: [id, subjectId, name, slug, sortOrder, active]
      properties:
        id: { type: string }
        subjectId: { type: string }
        name: { type: string, maxLength: 100 }
        slug: { type: string, maxLength: 100 }
        sortOrder: { type: integer }
        active: { type: boolean }
    DirectoryCreateRequest:
      type: object
      additionalProperties: false
      required: [name, slug]
      properties:
        name: { type: string, minLength: 1, maxLength: 100 }
        slug:
          type: string
          minLength: 1
          maxLength: 100
          pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$"
        sortOrder: { type: integer, minimum: -10000, maximum: 10000 }
        active: { type: boolean }
    DirectoryUpdateRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        name: { type: string, minLength: 1, maxLength: 100 }
        slug:
          type: string
          minLength: 1
          maxLength: 100
          pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$"
        sortOrder: { type: integer, minimum: -10000, maximum: 10000 }
        active: { type: boolean }
    KnowledgeSubject:
      type: object
      required: [id, name, slug, sortOrder, active]
      properties:
        id: { type: string }
        name: { type: string, maxLength: 100 }
        slug: { type: string, maxLength: 100 }
        sortOrder: { type: integer }
        active: { type: boolean }
    KnowledgeSubjectCreateRequest:
      type: object
      additionalProperties: false
      required: [name, slug]
      properties:
        name: { type: string, minLength: 2, maxLength: 100 }
        slug: { type: string, minLength: 2, maxLength: 100 }
    KnowledgeLibraryCreateRequest:
      type: object
      additionalProperties: false
      required: [name, subjectId, scope]
      properties:
        name: { type: string, minLength: 2, maxLength: 160 }
        subjectId: { type: string }
        scope: { $ref: "#/components/schemas/KnowledgeLibraryScope" }
    KnowledgeLibrary:
      type: object
      required: [id, name, scope, ownerId, subjectId, subject, aiEnabled, aiEnabledAt, active, createdAt, updatedAt, _count]
      properties:
        id: { type: string }
        name: { type: string, maxLength: 160 }
        scope: { $ref: "#/components/schemas/KnowledgeLibraryScope" }
        ownerId: { type: [string, "null"] }
        subjectId: { type: string }
        subject: { $ref: "#/components/schemas/Subject" }
        aiEnabled: { type: boolean }
        aiEnabledAt: { type: [string, "null"], format: date-time }
        active: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        _count:
          type: object
          required: [documents, chapters]
          properties:
            documents: { type: integer, minimum: 0 }
            chapters: { type: integer, minimum: 0 }
        reader:
          type: object
          required: [documentCount, h2Count, contentUpdatedAt]
          properties:
            documentCount: { type: integer, minimum: 0 }
            h2Count: { type: integer, minimum: 0 }
            contentUpdatedAt: { type: [string, "null"], format: date-time }
    KnowledgeLibraryDirectory:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/KnowledgeLibrary" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 50 }
    KnowledgeLibraryChapter:
      type: object
      additionalProperties: false
      required: [id, libraryId, name, slug, sortOrder]
      properties:
        id: { type: string }
        libraryId: { type: string }
        name: { type: string, maxLength: 100 }
        slug: { type: string, maxLength: 100 }
        sortOrder: { type: integer }
    KnowledgeLibraryChapterPage:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/KnowledgeLibraryChapter" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
    KnowledgeImportJob:
      type: object
      required: [id, libraryId, fileType, sourceName, sourceSize, status, stage, nodeCount, estimatedChunkCount, imageCount, warningCount, errorCount, sourceCleanupStatus, sourceCleanupAttempts, sourceCleanupNextAttemptAt, sourceCleanupError, sourceCleanedAt, cancelRequestedAt, expiresAt, createdAt, updatedAt]
      properties:
        id: { type: string }
        libraryId: { type: string }
        targetDocumentId: { type: [string, "null"] }
        fileType: { type: string, enum: [MARKDOWN, ZIP] }
        sourceName: { type: string }
        sourceSize: { type: integer, maximum: 209715200 }
        status: { $ref: "#/components/schemas/KnowledgeImportStatus" }
        stage: { type: string }
        progressCurrent: { type: integer, minimum: 0 }
        progressTotal: { type: integer, minimum: 0 }
        title: { type: [string, "null"] }
        nodeCount: { type: integer, minimum: 0 }
        estimatedChunkCount: { type: integer, minimum: 0 }
        imageCount: { type: integer, minimum: 0 }
        processedImageBytes: { type: integer, minimum: 0 }
        warningCount: { type: integer, minimum: 0 }
        errorCount: { type: integer, minimum: 0 }
        errorCode: { type: [string, "null"] }
        errorMessage: { type: [string, "null"] }
        sourceCleanupStatus: { $ref: "#/components/schemas/ImportSourceCleanupStatus" }
        sourceCleanupAttempts: { type: integer, minimum: 0, maximum: 5 }
        sourceCleanupNextAttemptAt: { type: [string, "null"], format: date-time }
        sourceCleanupError: { type: [string, "null"] }
        sourceCleanedAt: { type: [string, "null"], format: date-time }
        cancelRequestedAt: { type: [string, "null"], format: date-time }
        confirmedVersionId: { type: [string, "null"] }
        expiresAt: { type: string, format: date-time }
        confirmedAt: { type: [string, "null"], format: date-time }
        completedAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    KnowledgeImportJobPage:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/KnowledgeImportJob" } }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
    KnowledgeImportIssue:
      type: object
      required: [id, severity, code, message, entryPath, nodePath, line, column, createdAt]
      properties:
        id: { type: integer, minimum: 1 }
        severity: { type: string, enum: [ERROR, WARNING] }
        code: { type: string, maxLength: 80 }
        message: { type: string, maxLength: 500 }
        entryPath: { type: [string, "null"], maxLength: 500 }
        nodePath: { type: [string, "null"], maxLength: 500 }
        line: { type: [integer, "null"], minimum: 1 }
        column: { type: [integer, "null"], minimum: 1 }
        createdAt: { type: string, format: date-time }
    KnowledgeImportIssuePage:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/KnowledgeImportIssue" } }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
    KnowledgeImportStructure:
      type: object
      required: [items, nextCursor, total, images, titleMarkdown, replacement]
      properties:
        items:
          type: array
          items:
            type: object
            required: [level, title, titleMarkdown, path, chapterName, chunkCount]
            properties:
              level: { type: integer, minimum: 2, maximum: 6 }
              title: { type: string }
              titleMarkdown: { type: string, maxLength: 4096 }
              path: { type: string }
              chapterName: { type: string }
              chunkCount: { type: integer, minimum: 0 }
        nextCursor: { type: [integer, "null"], minimum: 0 }
        total: { type: integer, minimum: 0 }
        images:
          type: array
          items:
            type: object
            required: [entryPath, altText, nodePathHash, occurrenceIndex]
            properties:
              entryPath: { type: string }
              altText: { type: string, minLength: 1, maxLength: 300 }
              nodePathHash: { type: string, minLength: 64, maxLength: 64 }
              occurrenceIndex: { type: integer, minimum: 0 }
        titleMarkdown: { type: [string, "null"], maxLength: 4096 }
        replacement:
          oneOf:
            - { type: "null" }
            - { $ref: "#/components/schemas/KnowledgeImportReplacementPreview" }
    KnowledgeImportReplacementPreview:
      type: object
      additionalProperties: false
      required: [mode, targetDocumentId, targetDocumentTitle, targetDocumentCreatedAt, activeVersionId, matchedH2Titles]
      properties:
        mode: { type: string, enum: [CREATE, REPLACE] }
        targetDocumentId: { type: [string, "null"] }
        targetDocumentTitle: { type: [string, "null"] }
        targetDocumentCreatedAt: { type: [string, "null"], format: date-time }
        activeVersionId: { type: [string, "null"] }
        matchedH2Titles:
          type: array
          items: { type: string, maxLength: 200 }
    KnowledgeLibraryDocument:
      type: object
      required: [id, title, kind, status, activeVersionId, publishedAt, createdAt, updatedAt, _count]
      properties:
        id: { type: string }
        title: { type: string, maxLength: 200 }
        kind: { $ref: "#/components/schemas/KnowledgeKind" }
        status: { $ref: "#/components/schemas/ContentStatus" }
        activeVersionId: { type: [string, "null"] }
        publishedAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        _count:
          type: object
          required: [versions]
          properties: { versions: { type: integer, minimum: 0 } }
    KnowledgeLibraryDocumentList:
      type: object
      required: [items, total]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/KnowledgeLibraryDocument" } }
        total: { type: integer, minimum: 0 }
    KnowledgeVersion:
      type: object
      required: [id, version, origin, title, titleMarkdown, contentHash, imageManifestHash, indexStatus, nodeCount, chunkCount, vectorCount, imageCount, renderStatus, renderBlockVersion, renderBlockCount, mathCount, renderError, error, indexedAt, activatedAt, retiredAt, createdAt]
      properties:
        id: { type: string }
        version: { type: integer, minimum: 1 }
        origin: { type: string, enum: [IMPORT, REINDEX] }
        title: { type: string }
        titleMarkdown: { type: [string, "null"], maxLength: 4096 }
        contentHash: { type: string, minLength: 64, maxLength: 64 }
        imageManifestHash: { type: string, minLength: 64, maxLength: 64 }
        indexStatus: { $ref: "#/components/schemas/IndexStatus" }
        nodeCount: { type: integer, minimum: 0 }
        chunkCount: { type: integer, minimum: 0 }
        vectorCount: { type: integer, minimum: 0 }
        imageCount: { type: integer, minimum: 0 }
        renderStatus: { $ref: "#/components/schemas/KnowledgeRenderStatus" }
        renderBlockVersion: { type: [string, "null"] }
        renderBlockCount: { type: integer, minimum: 0 }
        mathCount: { type: integer, minimum: 0 }
        renderError: { type: [string, "null"] }
        error: { type: [string, "null"] }
        indexedAt: { type: [string, "null"], format: date-time }
        activatedAt: { type: [string, "null"], format: date-time }
        retiredAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
    KnowledgeVersionList:
      type: object
      required: [documentId, activeVersionId, items]
      properties:
        documentId: { type: string }
        activeVersionId: { type: [string, "null"] }
        items: { type: array, items: { $ref: "#/components/schemas/KnowledgeVersion" } }
    KnowledgeTreeNode:
      type: object
      required: [id, parentId, level, title, titleMarkdown, path, breadcrumb, sortOrder, libraryChapterId, _count]
      properties:
        id: { type: string }
        parentId: { type: [string, "null"] }
        level: { type: integer, minimum: 2, maximum: 6 }
        title: { type: string }
        titleMarkdown: { type: [string, "null"], maxLength: 4096 }
        path: { type: string }
        breadcrumb: { type: string }
        sortOrder: { type: integer, minimum: 0 }
        libraryChapterId: { type: string }
        _count:
          type: object
          required: [chunks, imageReferences]
          properties:
            chunks: { type: integer, minimum: 0 }
            imageReferences: { type: integer, minimum: 0 }
    KnowledgeTree:
      type: object
      required: [id, version, title, indexStatus, nodes]
      properties:
        id: { type: string }
        version: { type: integer, minimum: 1 }
        title: { type: string }
        indexStatus: { $ref: "#/components/schemas/IndexStatus" }
        nodes: { type: array, items: { $ref: "#/components/schemas/KnowledgeTreeNode" } }
    KnowledgeReaderManifest:
      type: object
      additionalProperties: false
      required: [libraryId, libraryName, scope, subject, readerRevision, headingCount, blockCount, imageCount, firstAnchor, hasContent]
      properties:
        libraryId: { type: string }
        libraryName: { type: string, maxLength: 160 }
        scope: { $ref: "#/components/schemas/KnowledgeLibraryScope" }
        subject:
          type: object
          additionalProperties: false
          required: [id, name, slug]
          properties:
            id: { type: string }
            name: { type: string }
            slug: { type: string }
        readerRevision: { type: string, minLength: 10, maxLength: 100 }
        headingCount: { type: integer, minimum: 0 }
        blockCount: { type: integer, minimum: 0 }
        imageCount: { type: integer, minimum: 0 }
        firstAnchor: { type: [string, "null"] }
        hasContent: { type: boolean }
    KnowledgeHeadingPathItem:
      type: object
      additionalProperties: false
      required: [nodeId, level, title, titleMarkdown]
      properties:
        nodeId: { type: string }
        level: { type: integer, minimum: 2, maximum: 6 }
        title: { type: string, maxLength: 200 }
        titleMarkdown: { type: string, maxLength: 4096 }
    KnowledgeReaderOutlineItem:
      type: object
      additionalProperties: false
      required: [nodeId, parentId, level, title, titleMarkdown, sequenceKey, previousNodeId, nextNodeId]
      properties:
        nodeId: { type: string }
        parentId: { type: [string, "null"] }
        level: { type: integer, minimum: 2, maximum: 6 }
        title: { type: string, maxLength: 200 }
        titleMarkdown: { type: string, maxLength: 4096 }
        sequenceKey: { type: string }
        previousNodeId: { type: [string, "null"] }
        nextNodeId: { type: [string, "null"] }
    KnowledgeReaderOutlineResponse:
      type: object
      additionalProperties: false
      required: [readerRevision, items, nextCursor, total]
      properties:
        readerRevision: { type: string, minLength: 10, maxLength: 100 }
        items:
          type: array
          maxItems: 200
          items: { $ref: "#/components/schemas/KnowledgeReaderOutlineItem" }
        nextCursor: { type: [string, "null"] }
        total: { type: integer, minimum: 0 }
    KnowledgeReaderImage:
      type: object
      additionalProperties: false
      required: [sourcePath, occurrenceIndex, photoId, contentUrl, altText, width, height]
      properties:
        sourcePath: { type: string }
        occurrenceIndex: { type: integer, minimum: 0 }
        photoId: { type: string }
        contentUrl: { type: string, pattern: "^/api/v1/media/images/" }
        altText: { type: string, minLength: 1, maxLength: 300 }
        width: { type: [integer, "null"], minimum: 1 }
        height: { type: [integer, "null"], minimum: 1 }
    KnowledgeReaderHeadingItem:
      type: object
      additionalProperties: false
      required: [kind, sequenceKey, nodeId, parentId, level, title, titleMarkdown]
      properties:
        kind: { type: string, const: HEADING }
        sequenceKey: { type: string }
        nodeId: { type: string }
        parentId: { type: [string, "null"] }
        level: { type: integer, minimum: 2, maximum: 6 }
        title: { type: string, maxLength: 200 }
        titleMarkdown: { type: string, maxLength: 4096 }
    KnowledgeReaderBlockItem:
      type: object
      additionalProperties: false
      required: [kind, sequenceKey, nodeId, renderBlockId, blockIndex, markdown, sourceHash, markdownBytes, mathCount, images]
      properties:
        kind: { type: string, const: BLOCK }
        sequenceKey: { type: string }
        nodeId: { type: string }
        renderBlockId: { type: string }
        blockIndex: { type: integer, minimum: 0 }
        markdown: { type: string }
        sourceHash: { type: string, minLength: 64, maxLength: 64 }
        markdownBytes: { type: integer, minimum: 0, maximum: 262144 }
        mathCount: { type: integer, minimum: 0 }
        images:
          type: array
          items: { $ref: "#/components/schemas/KnowledgeReaderImage" }
    KnowledgeReaderRangeItem:
      oneOf:
        - { $ref: "#/components/schemas/KnowledgeReaderHeadingItem" }
        - { $ref: "#/components/schemas/KnowledgeReaderBlockItem" }
      discriminator:
        propertyName: kind
        mapping:
          HEADING: "#/components/schemas/KnowledgeReaderHeadingItem"
          BLOCK: "#/components/schemas/KnowledgeReaderBlockItem"
    KnowledgeReaderRangeResponse:
      type: object
      additionalProperties: false
      required: [readerRevision, items, hasBefore, hasAfter, beforeAnchor, afterAnchor]
      properties:
        readerRevision: { type: string, minLength: 10, maxLength: 100 }
        items:
          type: array
          maxItems: 60
          items: { $ref: "#/components/schemas/KnowledgeReaderRangeItem" }
        hasBefore: { type: boolean }
        hasAfter: { type: boolean }
        beforeAnchor: { type: [string, "null"] }
        afterAnchor: { type: [string, "null"] }
    KnowledgeReaderContext:
      type: object
      additionalProperties: false
      required: [readerRevision, anchor, nodeId, renderBlockId, sequenceKey, headingPath]
      properties:
        readerRevision: { type: string, minLength: 10, maxLength: 100 }
        anchor: { type: string }
        nodeId: { type: string }
        renderBlockId: { type: [string, "null"] }
        sequenceKey: { type: string }
        headingPath:
          type: array
          minItems: 1
          maxItems: 5
          items: { $ref: "#/components/schemas/KnowledgeHeadingPathItem" }
    KnowledgePreviewManifest:
      type: object
      additionalProperties: false
      required: [documentId, versionId, libraryId, libraryName, previewRevision, title, titleMarkdown, headingCount, blockCount, imageCount, firstAnchor]
      properties:
        documentId: { type: string }
        versionId: { type: string }
        libraryId: { type: string }
        libraryName: { type: string, maxLength: 160 }
        previewRevision: { type: string, minLength: 10, maxLength: 100 }
        title: { type: string, maxLength: 200 }
        titleMarkdown: { type: string, maxLength: 4096 }
        headingCount: { type: integer, minimum: 0 }
        blockCount: { type: integer, minimum: 0 }
        imageCount: { type: integer, minimum: 0 }
        firstAnchor: { type: [string, "null"] }
    KnowledgeCitation:
      type: object
      additionalProperties: false
      required: [index, libraryId, libraryName, documentId, documentVersionId, libraryChapterId, nodeId, nodeTitle, nodeTitleMarkdown, headingPath, renderBlockId]
      properties:
        index: { type: integer, minimum: 1, maximum: 4 }
        libraryId: { type: string }
        libraryName: { type: string, maxLength: 160 }
        documentId: { type: string }
        documentVersionId: { type: string }
        libraryChapterId: { type: string }
        nodeId: { type: string }
        nodeTitle: { type: string, maxLength: 200 }
        nodeTitleMarkdown: { type: string, maxLength: 4096 }
        headingPath:
          type: array
          minItems: 1
          maxItems: 5
          items: { $ref: "#/components/schemas/KnowledgeHeadingPathItem" }
        renderBlockId: { type: string }
    KnowledgeImageAttachment:
      type: object
      additionalProperties: false
      required: [type, photoId, url, altText, documentId]
      properties:
        type: { type: string, const: image }
        photoId: { type: string }
        url: { type: string, pattern: "^/api/v1/media/images/" }
        altText: { type: string, maxLength: 300 }
        documentId: { type: string }
        nodeId: { type: string }
        renderBlockId: { type: string }
        citationIndex: { type: integer, minimum: 1, maximum: 4 }
    NewKnowledgeChatRequest:
      type: object
      additionalProperties: false
      required: [question, subjectId, knowledgeMode, libraryIds]
      properties:
        question: { type: string, minLength: 2, maxLength: 2000 }
        subjectId: { type: string, minLength: 1, maxLength: 191 }
        knowledgeMode: { $ref: "#/components/schemas/KnowledgeConversationMode" }
        libraryIds:
          type: array
          minItems: 1
          maxItems: 20
          uniqueItems: true
          items: { type: string, minLength: 1, maxLength: 191 }
        libraryChapterIds:
          type: array
          uniqueItems: true
          items: { type: string, minLength: 1, maxLength: 191 }
    ContinuedKnowledgeChatRequest:
      type: object
      additionalProperties: false
      required: [question, conversationId]
      properties:
        question: { type: string, minLength: 2, maxLength: 2000 }
        conversationId: { type: string, minLength: 1, maxLength: 191 }
    KnowledgeChatRequest:
      oneOf:
        - { $ref: "#/components/schemas/NewKnowledgeChatRequest" }
        - { $ref: "#/components/schemas/ContinuedKnowledgeChatRequest" }
    AiConversationLibrarySummary:
      type: object
      additionalProperties: false
      required: [id, name, scope]
      properties:
        id: { type: string }
        name: { type: string, maxLength: 160 }
        scope: { $ref: "#/components/schemas/KnowledgeLibraryScope" }
    AiConversationSubject:
      type: object
      additionalProperties: false
      required: [id, name]
      properties:
        id: { type: string }
        name: { type: string, maxLength: 100 }
    AiConversationSummary:
      type: object
      required:
        [id, title, subjectId, subject, knowledgeMode, libraries, messageCount, createdAt, updatedAt]
      properties:
        id: { type: string }
        title: { type: string }
        subjectId: { type: [string, "null"] }
        subject:
          oneOf:
            - { $ref: "#/components/schemas/AiConversationSubject" }
            - { type: "null" }
        knowledgeMode:
          oneOf:
            - { $ref: "#/components/schemas/KnowledgeConversationMode" }
            - { type: "null" }
        libraries:
          type: array
          items: { $ref: "#/components/schemas/AiConversationLibrarySummary" }
        messageCount: { type: integer, minimum: 0 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    AiConversationMessage:
      type: object
      additionalProperties: false
      required: [id, role, content, citations, attachments, model, createdAt]
      properties:
        id: { type: string }
        role: { type: string }
        content: { type: string }
        citations: true
        attachments: true
        model: { type: [string, "null"] }
        createdAt: { type: string, format: date-time }
    AiConversationLibraryChapter:
      type: object
      additionalProperties: false
      required: [id, name, libraryId]
      properties:
        id: { type: string }
        name: { type: string }
        libraryId: { type: string }
    AiConversationDetail:
      allOf:
        - { $ref: "#/components/schemas/AiConversationSummary" }
        - type: object
          required: [libraryChapters, messages, hasEarlierMessages]
          properties:
            libraryChapters:
              type: array
              items: { $ref: "#/components/schemas/AiConversationLibraryChapter" }
            messages:
              type: array
              maxItems: 200
              items: { $ref: "#/components/schemas/AiConversationMessage" }
            hasEarlierMessages: { type: boolean }
    AiConversationListResponse:
      type: object
      additionalProperties: false
      required: [items, nextCursor]
      properties:
        items:
          type: array
          maxItems: 50
          items: { $ref: "#/components/schemas/AiConversationSummary" }
        nextCursor: { type: [string, "null"] }
    AiConversationDeleteResult:
      type: object
      additionalProperties: false
      required: [id, deleted]
      properties:
        id: { type: string }
        deleted: { type: boolean, const: true }
    KnowledgeChatMeta:
      type: object
      additionalProperties: false
      required: [conversationId, citations, images]
      properties:
        conversationId: { type: string }
        citations:
          type: array
          maxItems: 4
          items: { $ref: "#/components/schemas/KnowledgeCitation" }
        images:
          type: array
          maxItems: 4
          items: { $ref: "#/components/schemas/KnowledgeImageAttachment" }
    KnowledgeCleanupPending:
      type: object
      required: [documentId, deleted, cleanupPending]
      properties:
        documentId: { type: string }
        deleted: { type: boolean, const: true }
        cleanupPending: { type: boolean, const: true }
    KnowledgeDocument:
      type: object
      required:
        [
          id,
          title,
          kind,
          indexStatus,
          sourceName,
          mimeType,
          fileSize,
          subject,
          publishedAt,
        ]
      properties:
        id: { type: string }
        title: { type: string, maxLength: 200 }
        kind: { $ref: "#/components/schemas/KnowledgeKind" }
        indexStatus: { $ref: "#/components/schemas/IndexStatus" }
        sourceName: { type: [string, "null"] }
        mimeType: { type: [string, "null"] }
        fileSize: { type: integer, minimum: 0, maximum: 209715200 }
        subject: { $ref: "#/components/schemas/KnowledgeSubject" }
        publishedAt: { type: [string, "null"], format: date-time }
        updatedAt: { type: string, format: date-time }
    KnowledgeDocumentDetail:
      allOf:
        - { $ref: "#/components/schemas/KnowledgeDocument" }
        - type: object
          required: [body]
          properties:
            body: { type: [string, "null"] }
    KnowledgeListResponse:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/KnowledgeDocument" }
        total: { type: integer, minimum: 0 }
    KnowledgeManageDocument:
      allOf:
        - { $ref: "#/components/schemas/KnowledgeDocument" }
        - type: object
          required: [status, createdAt, updatedAt]
          properties:
            status: { $ref: "#/components/schemas/ContentStatus" }
            createdAt: { type: string, format: date-time }
            updatedAt: { type: string, format: date-time }
    KnowledgeManageResponse:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/KnowledgeManageDocument" }
        total: { type: integer, minimum: 0 }
    KnowledgeDeleteResult:
      type: object
      required: [id, deleted, storageDeleted]
      properties:
        id: { type: string }
        deleted: { type: boolean, const: true }
        storageDeleted: { type: boolean }
    ApiError:
      type: object
      required: [statusCode, message]
      properties:
        statusCode: { type: integer }
        code: { type: string }
        message:
          oneOf:
            - { type: string }
            - { type: array, items: { type: string } }
        requestId: { type: string }
    Author:
      type: object
      required: [id, displayName]
      properties:
        id: { type: string }
        displayName: { type: string }
    User:
      type: object
      required: [id, displayName, role, status]
      properties:
        id: { type: string }
        displayName: { type: string }
        role: { $ref: "#/components/schemas/Role" }
        status: { $ref: "#/components/schemas/AccountStatus" }
        createdAt: { type: string, format: date-time }
    Member:
      allOf:
        - { $ref: "#/components/schemas/User" }
        - type: object
          required: [createdAt, approvedAt]
          properties:
            createdAt: { type: string, format: date-time }
            approvedAt: { type: [string, "null"], format: date-time }
    MemberList:
      type: object
      required: [items, total]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/Member" } }
        total: { type: integer, minimum: 0 }
    AuthSession:
      type: object
      required: [user, csrfToken]
      properties:
        user: { $ref: "#/components/schemas/User" }
        csrfToken: { type: string }
    MessageResponse:
      type: object
      required: [message]
      properties:
        message: { type: string }
    ChangePasswordRequest:
      type: object
      additionalProperties: false
      required: [currentPassword, newPassword]
      properties:
        currentPassword:
          { type: string, minLength: 1, maxLength: 128, writeOnly: true }
        newPassword:
          type: string
          minLength: 10
          maxLength: 128
          pattern: "^(?=.*[A-Za-z])(?=.*[0-9]).+$"
          writeOnly: true
    CreateInviteRequest:
      type: object
      additionalProperties: false
      required: [label, maxUses, expiresAt]
      properties:
        label: { type: string, minLength: 1, maxLength: 120 }
        maxUses: { type: integer, minimum: 1, maximum: 500 }
        expiresAt: { type: string, format: date-time }
    CreateInviteResponse:
      type: object
      required: [id, code, label, expiresAt, maxUses, usedCount]
      properties:
        id: { type: string }
        code: { type: string, writeOnly: true }
        label: { type: string }
        expiresAt: { type: string, format: date-time }
        maxUses: { type: integer, minimum: 1, maximum: 500 }
        usedCount: { type: integer, minimum: 0 }
    InviteState:
      type: string
      enum: [ACTIVE, EXPIRED, EXHAUSTED, REVOKED]
    InviteItem:
      type: object
      additionalProperties: false
      required:
        [id, label, active, state, maxUses, usedCount, expiresAt, revokedAt, createdAt]
      properties:
        id: { type: string }
        label: { type: string }
        active: { type: boolean }
        state: { $ref: "#/components/schemas/InviteState" }
        maxUses: { type: integer, minimum: 1 }
        usedCount: { type: integer, minimum: 0 }
        expiresAt: { type: string, format: date-time }
        revokedAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
    InviteListResponse:
      type: object
      additionalProperties: false
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/InviteItem" }
        total: { type: integer, minimum: 0 }
    UpdateUserRequest:
      type: object
      additionalProperties: false
      properties:
        role: { $ref: "#/components/schemas/Role" }
        status: { $ref: "#/components/schemas/AccountStatus" }
    ResetPasswordResponse:
      type: object
      required: [temporaryPassword, message]
      properties:
        temporaryPassword: { type: string, writeOnly: true }
        message: { type: string }
    NewsWriteRequest:
      type: object
      additionalProperties: false
      required: [title, summary, body, visibility]
      properties:
        title: { type: string, minLength: 2, maxLength: 160 }
        summary: { type: string, maxLength: 300 }
        body: { type: string, minLength: 1, maxLength: 100000 }
        visibility: { $ref: "#/components/schemas/Visibility" }
        status: { $ref: "#/components/schemas/NewsEditableStatus" }
    NewsUpdateRequest:
      type: object
      additionalProperties: false
      required: [title, summary, body, visibility]
      properties:
        title: { type: string, minLength: 2, maxLength: 160 }
        summary: { type: string, maxLength: 300 }
        body: { type: string, minLength: 1, maxLength: 100000 }
        visibility: { $ref: "#/components/schemas/Visibility" }
        status: { $ref: "#/components/schemas/NewsEditableStatus" }
        expectedUpdatedAt: { type: string, format: date-time }
    NewsVersionRequest:
      type: object
      additionalProperties: false
      properties:
        expectedUpdatedAt: { type: string, format: date-time }
    NewsSummary:
      type: object
      required:
        [
          id,
          title,
          summary,
          bodyFormat,
          visibility,
          status,
          publishedAt,
          createdAt,
          author,
        ]
      properties:
        id: { type: string }
        title: { type: string }
        summary: { type: string }
        bodyFormat: { type: string, const: HTML_V1 }
        visibility: { $ref: "#/components/schemas/Visibility" }
        status: { $ref: "#/components/schemas/ContentStatus" }
        publishedAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        author: { $ref: "#/components/schemas/Author" }
    NewsItem:
      allOf:
        - { $ref: "#/components/schemas/NewsSummary" }
        - type: object
          required: [body]
          properties:
            body: { type: string }
    NewsList:
      type: object
      required: [items, total]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/NewsItem" } }
        total: { type: integer, minimum: 0 }
    NewsPage:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items:
            oneOf:
              - { $ref: "#/components/schemas/NewsSummary" }
              - { $ref: "#/components/schemas/NewsItem" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
    NewsManagePage:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/NewsSummary" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
    NewsDeleteResult:
      type: object
      additionalProperties: false
      required: [id, deleted]
      properties:
        id: { type: string }
        deleted: { type: boolean, const: true }
    NewsRecord:
      type: object
      required:
        [
          id,
          title,
          summary,
          body,
          bodyFormat,
          visibility,
          status,
          authorId,
          publishedAt,
          createdAt,
          updatedAt,
          deletedAt,
        ]
      properties:
        id: { type: string }
        title: { type: string }
        summary: { type: string }
        body: { type: string }
        bodyFormat: { type: string, const: HTML_V1 }
        visibility: { $ref: "#/components/schemas/Visibility" }
        status: { $ref: "#/components/schemas/ContentStatus" }
        authorId: { type: string }
        publishedAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        deletedAt: { type: [string, "null"], format: date-time }
    AlbumWriteRequest:
      type: object
      additionalProperties: false
      required: [title, description]
      properties:
        title: { type: string, minLength: 1, maxLength: 160 }
        description: { type: string, maxLength: 2000 }
    AlbumRenameRequest:
      type: object
      additionalProperties: false
      required: [title]
      properties:
        title: { type: string, minLength: 1, maxLength: 160 }
    AlbumRecord:
      type: object
      required:
        [id, title, description, authorId, createdAt, updatedAt, archivedAt, deletedAt]
      properties:
        id: { type: string }
        title: { type: string }
        description: { type: string }
        authorId: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        archivedAt: { type: [string, "null"], format: date-time }
        deletedAt: { type: [string, "null"], format: date-time }
    PhotoItem:
      type: object
      required:
        [
          id,
          albumId,
          caption,
          mimeType,
          size,
          width,
          height,
          sortOrder,
          createdAt,
          url,
        ]
      properties:
        originalAvailable: { type: boolean, description: 是否保留原始上传字节；旧照片不可由展示图补造原图 }
        id: { type: string }
        albumId: { type: [string, "null"] }
        caption: { type: string }
        mimeType: { type: string }
        size: { type: integer, minimum: 0 }
        width: { type: [integer, "null"] }
        height: { type: [integer, "null"] }
        sortOrder: { type: integer }
        createdAt: { type: string, format: date-time }
        url: { type: string }
    AlbumItem:
      type: object
      required:
        [id, title, description, photoCount, photos, createdAt, updatedAt, archivedAt]
      properties:
        id: { type: string }
        title: { type: string }
        description: { type: string }
        photoCount: { type: integer, minimum: 0 }
        photos:
          type: array
          items: { $ref: "#/components/schemas/PhotoItem" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        archivedAt: { type: [string, "null"], format: date-time }
    AlbumListResponse:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AlbumItem" }
        total: { type: integer, minimum: 0 }
    AlbumSummary:
      type: object
      required:
        [id, title, description, coverUrl, photoCount, createdAt, updatedAt, archivedAt]
      properties:
        id: { type: string }
        title: { type: string }
        description: { type: string }
        coverUrl: { type: [string, "null"] }
        photoCount: { type: integer, minimum: 0 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        archivedAt: { type: [string, "null"], format: date-time }
    AlbumSummaryListResponse:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AlbumSummary" }
        total: { type: integer, minimum: 0 }
    AlbumPhotoPage:
      type: object
      additionalProperties: false
      required: [items, photoCount, nextCursor]
      properties:
        items:
          type: array
          maxItems: 100
          items: { $ref: "#/components/schemas/PhotoItem" }
        photoCount: { type: integer, minimum: 0 }
        nextCursor: { type: [string, "null"] }
    AlbumPhotoRemoveResult:
      type: object
      required: [id, removed, photoDeleted, objectDeleted]
      properties:
        id: { type: string }
        removed: { type: boolean, const: true }
        photoDeleted: { type: boolean }
        objectDeleted: { type: [boolean, "null"] }
    ForumThreadWriteRequest:
      type: object
      additionalProperties: false
      required: [title, body]
      properties:
        title: { type: string, minLength: 2, maxLength: 160 }
        body: { type: string, minLength: 1, maxLength: 50000 }
    ForumPostWriteRequest:
      type: object
      additionalProperties: false
      required: [body]
      properties:
        body: { type: string, minLength: 1, maxLength: 20000 }
    ForumReportWriteRequest:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        reason: { type: string, minLength: 2, maxLength: 500 }
    ForumModerationRequest:
      type: object
      additionalProperties: false
      properties:
        pinned: { type: boolean }
        locked: { type: boolean }
        hidden: { type: boolean }
    ForumPost:
      type: object
      required: [id, body, authorId, createdAt, author]
      properties:
        id: { type: string }
        threadId: { type: string }
        body: { type: string }
        authorId: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        author: { $ref: "#/components/schemas/Author" }
    ForumPostRecord:
      type: object
      required:
        [id, threadId, body, authorId, hidden, createdAt, updatedAt, deletedAt]
      properties:
        id: { type: string }
        threadId: { type: string }
        body: { type: string }
        authorId: { type: string }
        hidden: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        deletedAt: { type: [string, "null"], format: date-time }
    ForumThread:
      type: object
      required:
        [id, title, body, pinned, locked, hidden, authorId, createdAt, author]
      properties:
        id: { type: string }
        title: { type: string }
        body: { type: string }
        pinned: { type: boolean }
        locked: { type: boolean }
        hidden: { type: boolean }
        authorId: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        author: { $ref: "#/components/schemas/Author" }
        posts:
          { type: array, items: { $ref: "#/components/schemas/ForumPost" } }
        _count:
          type: object
          properties:
            posts: { type: integer, minimum: 0 }
    ForumThreadList:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          { type: array, items: { $ref: "#/components/schemas/ForumThread" } }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
    ForumThreadRecord:
      type: object
      required:
        [
          id,
          title,
          body,
          authorId,
          pinned,
          locked,
          hidden,
          createdAt,
          updatedAt,
          deletedAt,
        ]
      properties:
        id: { type: string }
        title: { type: string }
        body: { type: string }
        authorId: { type: string }
        pinned: { type: boolean }
        locked: { type: boolean }
        hidden: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        deletedAt: { type: [string, "null"], format: date-time }
    ForumReport:
      type: object
      required:
        [
          id,
          reason,
          reporterId,
          threadId,
          postId,
          resolvedAt,
          createdAt,
          reporter,
        ]
      properties:
        id: { type: string }
        reason: { type: string }
        reporterId: { type: string }
        threadId: { type: [string, "null"] }
        postId: { type: [string, "null"] }
        resolvedAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
        reporter: { $ref: "#/components/schemas/Author" }
    ForumReportList:
      type: object
      required: [items, total]
      properties:
        items:
          { type: array, items: { $ref: "#/components/schemas/ForumReport" } }
        total: { type: integer, minimum: 0 }
    ForumReportRecord:
      type: object
      required:
        [id, reason, reporterId, threadId, postId, resolvedAt, createdAt]
      properties:
        id: { type: string }
        reason: { type: string }
        reporterId: { type: string }
        threadId: { type: [string, "null"] }
        postId: { type: [string, "null"] }
        resolvedAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
    AuditLogItem:
      type: object
      required:
        [id, actorId, action, targetType, targetId, metadata, createdAt, actor]
      properties:
        id: { type: string }
        actorId: { type: [string, "null"] }
        action: { type: string }
        targetType: { type: string }
        targetId: { type: [string, "null"] }
        metadata: true
        createdAt: { type: string, format: date-time }
        actor:
          oneOf:
            - { $ref: "#/components/schemas/Author" }
            - { type: "null" }
    AuditLogList:
      type: object
      required: [items, total]
      properties:
        items:
          { type: array, items: { $ref: "#/components/schemas/AuditLogItem" } }
        total: { type: integer, minimum: 0 }
    AuditLogPage:
      type: object
      additionalProperties: false
      required: [items, total, nextCursor]
      properties:
        items:
          type: array
          maxItems: 100
          items: { $ref: "#/components/schemas/AuditLogItem" }
        total:
          type: integer
          minimum: 0
          description: 当前页返回条数；后续页由 nextCursor 表示。
        nextCursor: { type: [string, "null"] }
    QuestionType:
      type: string
      enum: [SINGLE, MULTIPLE, TRUE_FALSE, SHORT_ANSWER]
    QuizOption:
      type: object
      additionalProperties: false
      required: [id, text]
      properties:
        id: { type: string, minLength: 1, maxLength: 80 }
        text: { type: string, minLength: 1, maxLength: 2000 }
    QuizImage:
      type: object
      required: [id, caption, mimeType, size, width, height, url]
      properties:
        id: { type: string }
        caption: { type: string }
        mimeType: { type: string }
        size: { type: integer, minimum: 0 }
        width: { type: [integer, "null"] }
        height: { type: [integer, "null"] }
        url: { type: string }
    QuizPastPaper:
      type: object
      required: [id, title, subjectId, subject, year]
      properties:
        id: { type: string }
        title: { type: string }
        subjectId: { type: string }
        subject: { type: string }
        year: { type: [integer, "null"] }
    QuizQuestionSummary:
      type: object
      required:
        [
          id,
          type,
          gradingType,
          typeLabel,
          subjectId,
          subject,
          chapterIds,
          chapter,
          chapters,
          category,
          origin,
          prompt,
          options,
          images,
          isPastPaper,
          pastPaper,
          paperOrder,
          maxScore,
        ]
      properties:
        id: { type: string }
        type: { $ref: "#/components/schemas/QuestionType" }
        gradingType: { $ref: "#/components/schemas/QuestionType" }
        typeLabel: { type: string }
        subjectId: { type: string }
        subject: { type: string }
        chapterIds: { type: array, items: { type: string } }
        chapter: { type: string }
        chapters:
          type: array
          minItems: 1
          items: { $ref: "#/components/schemas/SubjectChapter" }
        category: { $ref: "#/components/schemas/QuizQuestionCategory" }
        origin: { $ref: "#/components/schemas/QuizQuestionOrigin" }
        prompt: { type: string }
        options:
          { type: array, items: { $ref: "#/components/schemas/QuizOption" } }
        images:
          { type: array, items: { $ref: "#/components/schemas/QuizImage" } }
        isPastPaper: { type: boolean }
        pastPaper:
          oneOf:
            - { $ref: "#/components/schemas/QuizPastPaper" }
            - { type: "null" }
        paperOrder: { type: [integer, "null"] }
        maxScore: { type: integer, minimum: 1 }
    QuizStartRequest:
      type: object
      additionalProperties: false
      properties:
        count: { type: integer, minimum: 1, maximum: 100 }
        subjectId: { type: string }
        chapterIds:
          type: array
          minItems: 1
          maxItems: 20
          uniqueItems: true
          items: { type: string }
        chapterMatch: { $ref: "#/components/schemas/ChapterMatch" }
        includeCrossChapter: { type: boolean, default: false }
        typeLabels:
          type: array
          minItems: 1
          maxItems: 20
          items: { type: string, minLength: 1, maxLength: 100 }
        types:
          description: 兼容旧客户端的判分类型过滤；新客户端使用 typeLabels。
          type: array
          minItems: 1
          maxItems: 4
          items: { $ref: "#/components/schemas/QuestionType" }
        source: { $ref: "#/components/schemas/QuizQuestionSourceFilter" }
        includePastPapers: { type: boolean, default: false }
        questionIds:
          type: array
          minItems: 1
          maxItems: 100
          uniqueItems: true
          items: { type: string }
      anyOf:
        - required: [count]
        - required: [questionIds]
    QuizStartResponse:
      type: object
      required: [attemptId, questions]
      properties:
        attemptId: { type: string }
        questions:
          type: array
          items: { $ref: "#/components/schemas/QuizQuestionSummary" }
    QuizAttemptStatus:
      type: string
      enum: [DRAFT, SCORING, SUBMITTED, SCORING_FAILED, ABANDONED]
    QuizAnswerMap:
      type: object
      additionalProperties:
        type: array
        items: { type: string }
    QuizAttemptLifecycle:
      type: object
      additionalProperties: false
      required: [attemptId, status, revision, position, answers, questions, savedAt, expiresAt, abandonedAt, submittedAt, score, total, results, gradingError]
      properties:
        attemptId: { type: string }
        status: { $ref: "#/components/schemas/QuizAttemptStatus" }
        revision: { type: integer, minimum: 0 }
        position: { type: integer, minimum: 0 }
        answers: { $ref: "#/components/schemas/QuizAnswerMap" }
        questions:
          type: array
          minItems: 1
          items: { $ref: "#/components/schemas/QuizQuestionSummary" }
        savedAt: { type: [string, "null"], format: date-time }
        expiresAt: { type: [string, "null"], format: date-time }
        abandonedAt: { type: [string, "null"], format: date-time }
        submittedAt: { type: [string, "null"], format: date-time }
        score: { type: [number, "null"], minimum: 0 }
        total: { type: integer, minimum: 0 }
        results:
          oneOf:
            - type: array
              items: { type: object, additionalProperties: true }
            - { type: "null" }
        gradingError: { type: [string, "null"], maxLength: 500 }
    QuizActiveAttemptList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 5
          items: { $ref: "#/components/schemas/QuizAttemptLifecycle" }
    QuizDraftSaveRequest:
      type: object
      additionalProperties: false
      required: [revision, position, answers]
      properties:
        revision: { type: integer, minimum: 0 }
        position: { type: integer, minimum: 0 }
        answers: { $ref: "#/components/schemas/QuizAnswerMap" }
    QuizDraftSaveResponse:
      type: object
      additionalProperties: false
      required: [attemptId, revision, position, savedAt, expiresAt]
      properties:
        attemptId: { type: string }
        revision: { type: integer, minimum: 1 }
        position: { type: integer, minimum: 0 }
        savedAt: { type: string, format: date-time }
        expiresAt: { type: string, format: date-time }
    QuizAbandonResponse:
      type: object
      additionalProperties: false
      required: [attemptId, abandoned]
      properties:
        attemptId: { type: string }
        abandoned: { type: boolean, const: true }
        abandonedAt: { type: string, format: date-time }
    QuizSubmitRequest:
      type: object
      additionalProperties: false
      required: [answers]
      properties:
        answers: { $ref: "#/components/schemas/QuizAnswerMap" }
    QuizSubmitPendingResponse:
      type: object
      additionalProperties: false
      required: [attemptId, status, pending]
      properties:
        attemptId: { type: string }
        status: { type: string, const: SCORING }
        pending: { type: boolean, const: true }
    QuizSubmitCompletedResponse:
      type: object
      additionalProperties: false
      required: [attemptId, status, pending, score, total, results]
      properties:
        attemptId: { type: string }
        status: { type: string, const: SUBMITTED }
        pending: { type: boolean, const: false }
        score: { type: number, minimum: 0 }
        total: { type: integer, minimum: 0 }
        results:
          type: array
          items: { type: object, additionalProperties: true }
        submittedAt: { type: [string, "null"], format: date-time }
    QuizSubjectTypeFilter:
      type: object
      required:
        [label, gradingTypes, total, randomEligibleCount, pastPaperCount]
      properties:
        label: { type: string }
        gradingTypes:
          type: array
          items: { $ref: "#/components/schemas/QuestionType" }
        total: { type: integer, minimum: 0 }
        randomEligibleCount: { type: integer, minimum: 0 }
        pastPaperCount: { type: integer, minimum: 0 }
    QuizPaperSummary:
      allOf:
        - { $ref: "#/components/schemas/QuizPastPaper" }
        - type: object
          required: [questionCount]
          properties:
            questionCount: { type: integer, minimum: 0 }
            typeLabels: { type: array, items: { type: string } }
            createdAt: { type: string, format: date-time }
    QuizChapterFilterGroup:
      type: object
      required:
        [chapterId, chapter, total, randomEligibleCount, pastPaperCount, types]
      properties:
        chapterId: { type: string }
        chapter: { type: string }
        total: { type: integer, minimum: 0 }
        randomEligibleCount: { type: integer, minimum: 0 }
        pastPaperCount: { type: integer, minimum: 0 }
        types:
          type: array
          items: { $ref: "#/components/schemas/QuizSubjectTypeFilter" }
    QuizSubjectFilterGroup:
      type: object
      required:
        [subjectId, subject, pastPaperCount, types, chapters, pastPapers]
      properties:
        subjectId: { type: string }
        subject: { type: string }
        pastPaperCount: { type: integer, minimum: 0 }
        types:
          type: array
          items: { $ref: "#/components/schemas/QuizSubjectTypeFilter" }
        chapters:
          type: array
          items: { $ref: "#/components/schemas/QuizChapterFilterGroup" }
        pastPapers:
          type: array
          items: { $ref: "#/components/schemas/QuizPaperSummary" }
    QuizFiltersResponse:
      type: object
      required: [subjects, types, subjectGroups]
      properties:
        subjects: { type: array, items: { type: string } }
        types:
          type: array
          items: { $ref: "#/components/schemas/QuestionType" }
        subjectGroups:
          type: array
          items: { $ref: "#/components/schemas/QuizSubjectFilterGroup" }
    QuizLibraryResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/QuizQuestionSummary" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1 }
    QuizPaperListResponse:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/QuizPaperSummary" }
        total: { type: integer, minimum: 0 }
    QuizHistoryItem:
      type: object
      required: [id, score, total, submittedAt, createdAt]
      properties:
        id: { type: string }
        score: { type: [number, "null"] }
        total: { type: integer, minimum: 0 }
        submittedAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }
    QuizHistoryResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/QuizHistoryItem" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
    QuizCriterionScore:
      type: object
      required: [description, awardedPoints, maxPoints, reason]
      properties:
        description: { type: string }
        awardedPoints: { type: integer, minimum: 0 }
        maxPoints: { type: integer, minimum: 1 }
        reason: { type: string }
    QuizWrongItem:
      type: object
      required:
        [
          id,
          type,
          subject,
          chapter,
          prompt,
          options,
          correctAnswer,
          maxScore,
          explanation,
          wrongCount,
          lastWrongAt,
        ]
      properties:
        id: { type: string }
        type: { $ref: "#/components/schemas/QuestionType" }
        gradingType: { $ref: "#/components/schemas/QuestionType" }
        typeLabel: { type: string }
        subject: { type: string }
        chapter: { type: string }
        prompt: { type: string }
        options:
          type: array
          items: { $ref: "#/components/schemas/QuizOption" }
        images:
          type: array
          items: { $ref: "#/components/schemas/QuizImage" }
        pastPaper:
          oneOf:
            - { $ref: "#/components/schemas/QuizPastPaper" }
            - { type: "null" }
        paperOrder: { type: [integer, "null"] }
        correctAnswer: { type: array, items: { type: string } }
        maxScore: { type: integer, minimum: 1 }
        explanation: { type: string }
        lastScore: { type: number, minimum: 0 }
        lastWrongAnswer: { type: string }
        lastFeedback: { type: string }
        criterionScores:
          type: array
          items: { $ref: "#/components/schemas/QuizCriterionScore" }
        wrongCount: { type: integer, minimum: 1 }
        lastWrongAt: { type: [string, "null"], format: date-time }
    QuizWrongResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/QuizWrongItem" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
    GradingCriterion:
      type: object
      additionalProperties: false
      required: [description, points]
      properties:
        description: { type: string, minLength: 2, maxLength: 1000 }
        points: { type: integer, minimum: 1, maximum: 100 }
    GradingRubric:
      type: object
      additionalProperties: false
      required: [criteria]
      properties:
        criteria:
          type: array
          minItems: 1
          maxItems: 20
          items: { $ref: "#/components/schemas/GradingCriterion" }
        notes: { type: string, minLength: 1, maxLength: 2000 }
    QuizQuestionWriteRequest:
      type: object
      additionalProperties: false
      required:
        [
          gradingType,
          subjectId,
          chapterIds,
          prompt,
          options,
          correctAnswer,
          explanation,
        ]
      properties:
        gradingType: { $ref: "#/components/schemas/QuestionType" }
        typeLabel: { type: string, minLength: 1, maxLength: 100 }
        subjectId: { type: string }
        chapterIds:
          type: array
          minItems: 1
          maxItems: 20
          uniqueItems: true
          items: { type: string }
        category: { $ref: "#/components/schemas/QuizQuestionCategory" }
        prompt: { type: string, minLength: 2, maxLength: 10000 }
        options:
          type: array
          items: { $ref: "#/components/schemas/QuizOption" }
        correctAnswer: { type: array, items: { type: string } }
        gradingRubric: { $ref: "#/components/schemas/GradingRubric" }
        explanation: { type: string, maxLength: 10000 }
        pastPaperId: { type: string }
        paperOrder: { type: integer, minimum: 1, maximum: 10000 }
    QuizQuestionEditorData:
      allOf:
        - { $ref: "#/components/schemas/QuizQuestionSummary" }
        - type: object
          required: [correctAnswer, explanation]
          properties:
            correctAnswer: { type: array, items: { type: string } }
            gradingRubric: { $ref: "#/components/schemas/GradingRubric" }
            explanation: { type: string }
    QuizQuestionDeleteResult:
      type: object
      required: [id, deleted]
      properties:
        id: { type: string }
        deleted: { type: boolean, const: true }
    QuizImportMode:
      type: string
      enum: [BANK, NEW_PAPER, APPEND_PAPER]
    QuizImportStatus:
      type: string
      enum: [PREFLIGHT_PENDING, PREFLIGHTING, AWAITING_CONFIRMATION, INVALID, IMPORT_PENDING, IMPORTING, COMPLETED, FAILED, COMPENSATION_FAILED, EXPIRED]
    QuizImportIssue:
      type: object
      required: [id, severity, code, rowNumber, filePath, field, message, createdAt]
      properties:
        id: { type: integer }
        severity: { type: string, enum: [ERROR, WARNING] }
        code: { type: string }
        rowNumber: { oneOf: [{ type: integer }, { type: "null" }] }
        filePath: { oneOf: [{ type: string }, { type: "null" }] }
        field: { oneOf: [{ type: string }, { type: "null" }] }
        message: { type: string }
        createdAt: { type: string, format: date-time }
    QuizImportJob:
      type: object
      required: [id, fileType, mode, sourceName, sourceSize, status, stage, progressCurrent, progressTotal, questionCount, imageCount, warningCount, errorCount, summary, importedCount, createdChapterCount, resultPastPaperId, errorCode, errorMessage, cancelRequestedAt, sourceCleanupStatus, sourceCleanupAttempts, sourceCleanupNextAttemptAt, sourceCleanupError, sourceCleanedAt, expiresAt, confirmedAt, completedAt, createdAt, updatedAt]
      properties:
        id: { type: string }
        fileType: { type: string, enum: [CSV, ZIP] }
        mode: { $ref: "#/components/schemas/QuizImportMode" }
        sourceName: { type: string }
        sourceSize: { type: integer }
        status: { $ref: "#/components/schemas/QuizImportStatus" }
        stage: { type: string }
        progressCurrent: { type: integer, minimum: 0 }
        progressTotal: { type: integer, minimum: 0 }
        questionCount: { type: integer, minimum: 0 }
        imageCount: { type: integer, minimum: 0 }
        warningCount: { type: integer, minimum: 0 }
        errorCount: { type: integer, minimum: 0 }
        summary: { oneOf: [{ type: object, additionalProperties: true }, { type: "null" }] }
        importedCount: { type: integer, minimum: 0 }
        createdChapterCount: { type: integer, minimum: 0 }
        resultPastPaperId: { oneOf: [{ type: string }, { type: "null" }] }
        errorCode: { oneOf: [{ type: string }, { type: "null" }] }
        errorMessage: { oneOf: [{ type: string }, { type: "null" }] }
        cancelRequestedAt: { type: [string, "null"], format: date-time }
        sourceCleanupStatus: { $ref: "#/components/schemas/ImportSourceCleanupStatus" }
        sourceCleanupAttempts: { type: integer, minimum: 0, maximum: 5 }
        sourceCleanupNextAttemptAt: { type: [string, "null"], format: date-time }
        sourceCleanupError: { type: [string, "null"] }
        sourceCleanedAt: { type: [string, "null"], format: date-time }
        expiresAt: { type: string, format: date-time }
        confirmedAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        completedAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    QuizImportJobDetail:
      allOf:
        - { $ref: "#/components/schemas/QuizImportJob" }
        - type: object
          properties:
            issues:
              type: array
              items: { $ref: "#/components/schemas/QuizImportIssue" }
            issuesTruncated: { type: boolean }
            resultPastPaper:
              oneOf:
                - { $ref: "#/components/schemas/QuizPastPaper" }
                - { type: "null" }
    QuizImportListResponse:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/QuizImportJob" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1 }
    UserPracticeInitializationStatus:
      type: string
      enum: [PENDING, PROCESSING, READY, FAILED]
    DailyPracticeCycleStatus:
      type: string
      enum: [BUILDING, GENERATING, READY, PAUSED, DEGRADED, FAILED]
    DailyPracticeDayStatus:
      type: string
      enum: [PENDING, PROCESSING, READY, LIMITED_CONTENT, NO_CONTENT, DEGRADED_READY, FAILED, PAUSED, STARTED, COMPLETED, STALE]
    DailyPracticeTodayStatus:
      type: string
      enum: [SERVICE_PAUSED, INITIALIZING, NO_TEACHING_PROGRESS, GENERATING, PENDING, PROCESSING, READY, LIMITED_CONTENT, NO_CONTENT, DEGRADED_READY, FAILED, PAUSED, STARTED, COMPLETED, STALE]
    DailyPracticeGenerationSource:
      type: string
      enum: [PRO_MAX, PRO_HIGH, FLASH_HIGH, DETERMINISTIC, NO_MODEL]
    DailyPracticeSuggestionStatus:
      type: string
      enum: [PENDING, APPLIED, PARTIALLY_APPLIED, NOT_APPLIED, SUPERSEDED, EXPIRED_SERVICE_PAUSED]
    DailyPracticePlanTrigger:
      type: string
      enum: [AUTO, ADMIN_REGENERATE, ADMIN_PREVIEW]
    DailyPracticeIntensity:
      type: string
      enum: [LIGHT, STANDARD, CHALLENGING]
    DailyPracticeDataQuality:
      type: string
      enum: [SUFFICIENT, LIMITED, NONE]
    DailyPracticeServiceState:
      type: object
      additionalProperties: false
      required: [enabled, paused, reason, resumesAt, settingsRevision]
      properties:
        enabled: { type: boolean }
        paused: { type: boolean }
        reason: { oneOf: [{ type: string }, { type: "null" }] }
        resumesAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        settingsRevision: { type: integer, minimum: 0 }
    DailyPracticeLearningSummaryEntry:
      type: object
      additionalProperties: false
      required: [knowledgeAlias, text, evidenceRefs]
      properties:
        knowledgeAlias: { type: string }
        text: { type: string }
        evidenceRefs:
          type: array
          uniqueItems: true
          items: { type: string }
    DailyPracticeLearningSummary:
      type: object
      additionalProperties: false
      required: [headline, overview, dataQuality, strengths, priorities]
      properties:
        headline: { type: string }
        overview: { type: string }
        dataQuality: { $ref: "#/components/schemas/DailyPracticeDataQuality" }
        strengths:
          type: array
          maxItems: 3
          items: { $ref: "#/components/schemas/DailyPracticeLearningSummaryEntry" }
        priorities:
          type: array
          maxItems: 5
          items: { $ref: "#/components/schemas/DailyPracticeLearningSummaryEntry" }
    DailyPracticePlanItem:
      type: object
      additionalProperties: false
      required: [ordinal, source, questionId, gradingType, typeLabel, subjectId, subject, chapterIds, chapters, prompt, options, images, maxScore, reason, evidenceRefs]
      properties:
        ordinal: { type: integer, minimum: 0 }
        source: { type: string, enum: [PERSONALIZED, ADMIN_FIXED] }
        questionId: { type: string }
        gradingType: { $ref: "#/components/schemas/QuestionType" }
        typeLabel: { type: string }
        subjectId: { type: string }
        subject: { type: string }
        chapterIds:
          type: array
          items: { type: string }
        chapters:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [id, name, slug]
            properties:
              id: { type: string }
              name: { type: string }
              slug: { type: string }
        prompt: { type: string }
        options:
          type: array
          items: { $ref: "#/components/schemas/QuizOption" }
        images:
          type: array
          items: { $ref: "#/components/schemas/QuizImage" }
        maxScore: { type: integer, minimum: 1 }
        reason: { type: string }
        evidenceRefs:
          type: array
          uniqueItems: true
          items: { type: string }
    DailyPracticeSuggestionRequest:
      type: object
      additionalProperties: false
      required: [intensity, desiredQuestionCount, focusSubjectIds, focusChapterIds]
      properties:
        intensity: { $ref: "#/components/schemas/DailyPracticeIntensity" }
        desiredQuestionCount: { type: integer, minimum: 5, maximum: 10 }
        focusSubjectIds:
          type: array
          maxItems: 2
          uniqueItems: true
          items: { type: string, minLength: 1, maxLength: 191 }
        focusChapterIds:
          type: array
          maxItems: 5
          uniqueItems: true
          items: { type: string, minLength: 1, maxLength: 191 }
        note: { type: string, minLength: 1, maxLength: 300 }
    DailyPracticeSuggestion:
      type: object
      additionalProperties: false
      required: [id, targetPracticeDate, payload, status, submittedByRole, createdAt]
      properties:
        id: { type: string }
        targetPracticeDate: { type: string, format: date }
        payload: { $ref: "#/components/schemas/DailyPracticeSuggestionRequest" }
        status: { $ref: "#/components/schemas/DailyPracticeSuggestionStatus" }
        submittedByRole: { $ref: "#/components/schemas/Role" }
        createdAt: { type: string, format: date-time }
    DailyPracticeAttemptSummary:
      type: object
      additionalProperties: false
      required: [id, submittedAt, score, total]
      properties:
        id: { type: string }
        submittedAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        score: { oneOf: [{ type: integer, minimum: 0 }, { type: "null" }] }
        total: { type: integer, minimum: 0 }
    DailyPracticeToday:
      type: object
      additionalProperties: false
      required: [practiceDate, timeZone, dayStartedAt, nextDayStartsAt, status, service, dayId, supplemental, planRevisionId, generationSource, generatedAt, degradedReason, summary, personalizedItems, fixedItems, counts, attempt, suggestion, initialization]
      properties:
        practiceDate: { type: string, format: date }
        timeZone: { type: string, const: Asia/Shanghai }
        dayStartedAt: { type: string, format: date-time }
        nextDayStartsAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/DailyPracticeTodayStatus" }
        service: { $ref: "#/components/schemas/DailyPracticeServiceState" }
        dayId: { oneOf: [{ type: string }, { type: "null" }] }
        supplemental: { type: boolean }
        planRevisionId: { oneOf: [{ type: string }, { type: "null" }] }
        generationSource:
          oneOf:
            - { $ref: "#/components/schemas/DailyPracticeGenerationSource" }
            - { type: "null" }
        generatedAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        degradedReason: { oneOf: [{ type: string }, { type: "null" }] }
        summary:
          oneOf:
            - { $ref: "#/components/schemas/DailyPracticeLearningSummary" }
            - { type: "null" }
        personalizedItems:
          type: array
          items: { $ref: "#/components/schemas/DailyPracticePlanItem" }
        fixedItems:
          type: array
          items: { $ref: "#/components/schemas/DailyPracticePlanItem" }
        counts:
          type: object
          additionalProperties: false
          required: [personalized, fixed, total]
          properties:
            personalized: { type: integer, minimum: 0 }
            fixed: { type: integer, minimum: 0 }
            total: { type: integer, minimum: 0 }
        attempt:
          oneOf:
            - { $ref: "#/components/schemas/DailyPracticeAttemptSummary" }
            - { type: "null" }
        suggestion:
          type: object
          additionalProperties: false
          required: [targetPracticeDate, available, current]
          properties:
            targetPracticeDate: { type: string, format: date }
            available: { type: boolean }
            current:
              oneOf:
                - { $ref: "#/components/schemas/DailyPracticeSuggestion" }
                - { type: "null" }
        initialization:
          oneOf:
            - type: object
              additionalProperties: false
              required: [status, appliedAttempts, totalAttempts]
              properties:
                status: { $ref: "#/components/schemas/UserPracticeInitializationStatus" }
                appliedAttempts: { type: integer, minimum: 0 }
                totalAttempts: { type: integer, minimum: 0 }
            - { type: "null" }
    DailyPracticeStartResponse:
      allOf:
        - { $ref: "#/components/schemas/QuizStartResponse" }
        - type: object
          required: [planRevisionId]
          properties:
            planRevisionId: { type: string }
    DailyPracticeHistoryItem:
      type: object
      required: [planRevisionId, practiceDate, status, generationSource, generatedAt, summary, questionCount, fixedQuestionCount, attempt]
      properties:
        planRevisionId: { type: string }
        practiceDate: { type: string, format: date }
        status: { $ref: "#/components/schemas/DailyPracticeDayStatus" }
        generationSource: { $ref: "#/components/schemas/DailyPracticeGenerationSource" }
        generatedAt: { type: string, format: date-time }
        summary:
          oneOf:
            - { $ref: "#/components/schemas/DailyPracticeLearningSummary" }
            - { type: "null" }
        questionCount: { type: integer, minimum: 0 }
        fixedQuestionCount: { type: integer, minimum: 0 }
        attempt:
          oneOf:
            - { $ref: "#/components/schemas/DailyPracticeAttemptSummary" }
            - { type: "null" }
    DailyPracticeHistoryPage:
      type: object
      additionalProperties: false
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/DailyPracticeHistoryItem" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
    DailyPracticeHistoryDetail:
      allOf:
        - { $ref: "#/components/schemas/DailyPracticeHistoryItem" }
        - type: object
          required: [personalizedItems, fixedItems, resultSummary]
          properties:
            personalizedItems:
              type: array
              items: { $ref: "#/components/schemas/DailyPracticePlanItem" }
            fixedItems:
              type: array
              items: { $ref: "#/components/schemas/DailyPracticePlanItem" }
            resultSummary: { oneOf: [{ type: object, additionalProperties: true }, { type: "null" }] }
    DailyPracticeSettings:
      type: object
      additionalProperties: false
      required: [enabled, revision, updatedAt, updatedBy, effective]
      properties:
        enabled: { type: boolean }
        revision: { type: integer, minimum: 0 }
        updatedAt: { type: string, format: date-time }
        updatedBy:
          oneOf:
            - { $ref: "#/components/schemas/Author" }
            - { type: "null" }
        effective: { $ref: "#/components/schemas/DailyPracticeServiceState" }
    DailyPracticeSettingsUpdateRequest:
      type: object
      additionalProperties: false
      required: [enabled, expectedRevision, reason]
      properties:
        enabled: { type: boolean }
        expectedRevision: { type: integer, minimum: 0 }
        reason: { type: string, minLength: 1, maxLength: 200 }
    DailyPracticeServicePause:
      type: object
      additionalProperties: false
      required: [id, startsAt, endsAt, reason, cancelledAt, createdAt, createdBy, cancelledBy]
      properties:
        id: { type: string }
        startsAt: { type: string, format: date-time }
        endsAt: { type: string, format: date-time }
        reason: { type: string }
        cancelledAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        createdAt: { type: string, format: date-time }
        createdBy:
          oneOf:
            - { $ref: "#/components/schemas/Author" }
            - { type: "null" }
        cancelledBy:
          oneOf:
            - { $ref: "#/components/schemas/Author" }
            - { type: "null" }
    DailyPracticeServicePauseCreateRequest:
      type: object
      additionalProperties: false
      required: [startsAt, endsAt, reason]
      properties:
        startsAt: { type: string, format: date-time }
        endsAt: { type: string, format: date-time }
        reason: { type: string, minLength: 1, maxLength: 200 }
    DailyPracticeServicePausePage:
      type: object
      additionalProperties: false
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/DailyPracticeServicePause" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
    TeachingProgressChangeType:
      type: string
      enum: [INITIAL, ADD, CORRECTION]
    TeachingProgressNodeInput:
      type: object
      additionalProperties: false
      required: [knowledgeNodeId, firstTaughtDate]
      properties:
        knowledgeNodeId: { type: string, minLength: 1, maxLength: 191 }
        firstTaughtDate: { type: string, format: date }
    TeachingProgressNodeSnapshot:
      type: object
      additionalProperties: false
      required: [id, libraryId, documentId, nodePathHash, currentKnowledgeNodeId, title, breadcrumb, firstTaughtDate, resolved, remapped, resolvedDocumentId]
      properties:
        id: { type: string }
        libraryId: { type: string }
        documentId: { type: string }
        nodePathHash: { type: string, minLength: 64, maxLength: 64 }
        currentKnowledgeNodeId: { oneOf: [{ type: string }, { type: "null" }] }
        title: { type: string }
        breadcrumb: { type: string }
        firstTaughtDate: { type: string, format: date }
        resolved: { type: boolean }
        remapped: { type: boolean }
        resolvedDocumentId: { oneOf: [{ type: string }, { type: "null" }] }
    TeachingProgressSummary:
      type: object
      required: [id, subject, version, effectivePracticeDate, changeType, note, correctionReason, scopeHash, nodeCount, publishedBy, publishedAt]
      properties:
        id: { type: string }
        subject: { $ref: "#/components/schemas/Subject" }
        version: { type: integer, minimum: 1 }
        effectivePracticeDate: { type: string, format: date }
        changeType: { $ref: "#/components/schemas/TeachingProgressChangeType" }
        note: { oneOf: [{ type: string }, { type: "null" }] }
        correctionReason: { oneOf: [{ type: string }, { type: "null" }] }
        scopeHash: { type: string, minLength: 64, maxLength: 64 }
        nodeCount: { type: integer, minimum: 0 }
        publishedBy:
          oneOf:
            - { $ref: "#/components/schemas/Author" }
            - { type: "null" }
        publishedAt: { type: string, format: date-time }
    TeachingProgressDetail:
      allOf:
        - { $ref: "#/components/schemas/TeachingProgressSummary" }
        - type: object
          required: [basedOnProgressId, nodes, unresolvedNodeCount, eligibleQuestionCount]
          properties:
            basedOnProgressId: { oneOf: [{ type: string }, { type: "null" }] }
            nodes:
              type: array
              maxItems: 5000
              items: { $ref: "#/components/schemas/TeachingProgressNodeSnapshot" }
            unresolvedNodeCount: { type: integer, minimum: 0 }
            eligibleQuestionCount: { type: integer, minimum: 0 }
    TeachingProgressPublishRequest:
      type: object
      additionalProperties: false
      required: [subjectId, effectivePracticeDate, changeType, expectedVersion, nodes]
      properties:
        subjectId: { type: string, minLength: 1, maxLength: 191 }
        effectivePracticeDate: { type: string, format: date }
        changeType: { $ref: "#/components/schemas/TeachingProgressChangeType" }
        basedOnProgressId: { type: string, minLength: 1, maxLength: 191 }
        expectedVersion: { type: integer, minimum: 0 }
        note: { type: string, minLength: 1, maxLength: 1000 }
        correctionReason: { type: string, minLength: 1, maxLength: 1000 }
        nodes:
          type: array
          maxItems: 5000
          items: { $ref: "#/components/schemas/TeachingProgressNodeInput" }
    TeachingProgressPage:
      type: object
      additionalProperties: false
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/TeachingProgressSummary" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
    DailyPracticeFixedQuestion:
      type: object
      additionalProperties: false
      required: [id, gradingType, typeLabel, subjectId, subject, chapterIds, chapters, prompt, origin, isPastPaper]
      properties:
        id: { type: string }
        gradingType: { $ref: "#/components/schemas/QuestionType" }
        typeLabel: { type: string }
        subjectId: { type: string }
        subject: { type: string }
        chapterIds:
          type: array
          items: { type: string }
        chapters:
          type: array
          items:
            type: object
            required: [id, name, slug]
            properties:
              id: { type: string }
              name: { type: string }
              slug: { type: string }
        prompt: { type: string }
        origin: { type: string, enum: [MANUAL, CSV] }
        isPastPaper: { type: boolean }
        ordinal: { type: integer, minimum: 0 }
    DailyPracticeFixedQuestionPage:
      type: object
      additionalProperties: false
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/DailyPracticeFixedQuestion" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
    DailyPracticeFixedAssignment:
      type: object
      additionalProperties: false
      required: [id, practiceDate, revision, basedOnAssignmentId, assignmentHash, note, publishedAt, publishedBy, questions]
      properties:
        id: { type: string }
        practiceDate: { type: string, format: date }
        revision: { type: integer, minimum: 1 }
        basedOnAssignmentId: { oneOf: [{ type: string }, { type: "null" }] }
        assignmentHash: { type: string, minLength: 64, maxLength: 64 }
        note: { oneOf: [{ type: string }, { type: "null" }] }
        publishedAt: { type: string, format: date-time }
        publishedBy:
          oneOf:
            - { $ref: "#/components/schemas/Author" }
            - { type: "null" }
        questions:
          type: array
          maxItems: 20
          items: { $ref: "#/components/schemas/DailyPracticeFixedQuestion" }
    DailyPracticeFixedAssignmentEnvelope:
      type: object
      additionalProperties: false
      required: [id, practiceDate, revision, basedOnAssignmentId, assignmentHash, note, publishedAt, publishedBy, questions, history, historyTotal]
      properties:
        id: { type: string }
        practiceDate: { type: string, format: date }
        revision: { type: integer, minimum: 1 }
        basedOnAssignmentId: { oneOf: [{ type: string }, { type: "null" }] }
        assignmentHash: { type: string, minLength: 64, maxLength: 64 }
        note: { oneOf: [{ type: string }, { type: "null" }] }
        publishedAt: { type: string, format: date-time }
        publishedBy:
          oneOf:
            - { $ref: "#/components/schemas/Author" }
            - { type: "null" }
        questions:
          type: array
          maxItems: 20
          items: { $ref: "#/components/schemas/DailyPracticeFixedQuestion" }
        history:
          type: array
          maxItems: 50
          items: { $ref: "#/components/schemas/DailyPracticeFixedAssignment" }
        historyTotal: { type: integer, minimum: 0 }
    DailyPracticeFixedAssignmentPublishRequest:
      type: object
      additionalProperties: false
      required: [practiceDate, expectedRevision, questionIds]
      properties:
        practiceDate: { type: string, format: date }
        expectedRevision: { type: integer, minimum: 0 }
        questionIds:
          type: array
          maxItems: 20
          uniqueItems: true
          items: { type: string, minLength: 1, maxLength: 191 }
        note: { type: string, minLength: 1, maxLength: 1000 }
    DailyPracticeCycleAggregate:
      type: object
      additionalProperties: false
      required: [practiceDate, status, baselineAt, deadlineAt, totalUsers, statusCounts, generationCounts, progressPercent, latencyMs, usage, gapSummary, invalidFixedQuestionCount, progressSetHash, refreezeRequestedAt, pool]
      properties:
        practiceDate: { type: string, format: date }
        status: { $ref: "#/components/schemas/DailyPracticeCycleStatus" }
        baselineAt: { type: string, format: date-time }
        deadlineAt: { type: string, format: date-time }
        totalUsers: { type: integer, minimum: 0 }
        statusCounts:
          type: object
          additionalProperties: { type: integer, minimum: 0 }
        generationCounts:
          type: object
          additionalProperties: { type: integer, minimum: 0 }
        progressPercent: { type: number, minimum: 0, maximum: 100 }
        latencyMs:
          type: object
          additionalProperties: false
          required: [p50, p95]
          properties:
            p50: { oneOf: [{ type: number, minimum: 0 }, { type: "null" }] }
            p95: { oneOf: [{ type: number, minimum: 0 }, { type: "null" }] }
        usage:
          type: object
          additionalProperties: false
          required: [calls, inputTokens, outputTokens]
          properties:
            calls: { type: integer, minimum: 0 }
            inputTokens: { type: string, pattern: "^[0-9]+$" }
            outputTokens: { type: string, pattern: "^[0-9]+$" }
        gapSummary:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [subjectId, subject, nodeCount, eligibleQuestionCount]
            properties:
              subjectId: { type: string }
              subject: { type: string }
              nodeCount: { type: integer, minimum: 0 }
              eligibleQuestionCount: { type: integer, minimum: 0 }
        invalidFixedQuestionCount: { type: integer, minimum: 0 }
        progressSetHash: { type: string, minLength: 64, maxLength: 64 }
        refreezeRequestedAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        pool:
          type: object
          additionalProperties: false
          required: [progressNodeCount, candidateQuestionCount, candidateTypeCounts, unresolvedProgressNodeCount, remappedProgressNodeCount, remappedNodes]
          properties:
            progressNodeCount: { type: integer, minimum: 0 }
            candidateQuestionCount: { type: integer, minimum: 0 }
            candidateTypeCounts:
              type: object
              additionalProperties: { type: integer, minimum: 0 }
            unresolvedProgressNodeCount: { type: integer, minimum: 0 }
            remappedProgressNodeCount: { type: integer, minimum: 0 }
            remappedNodes:
              type: array
              maxItems: 50
              items:
                type: object
                additionalProperties: false
                required: [subjectId, title, breadcrumb, documentId, remappedFromDocumentId, firstTaughtDate]
                properties:
                  subjectId: { type: string }
                  title: { type: string }
                  breadcrumb: { type: string }
                  documentId: { type: string }
                  remappedFromDocumentId: { type: string }
                  firstTaughtDate: { type: string, format: date }
    DailyPracticeCycleSummary:
      type: object
      additionalProperties: false
      required: [practiceDate, status, baselineAt, deadlineAt, refreezeRequestedAt, activeUsers, createdDays, terminalUsers, statusCounts, pool]
      properties:
        practiceDate: { type: string, format: date }
        status: { $ref: "#/components/schemas/DailyPracticeCycleStatus" }
        baselineAt: { type: string, format: date-time }
        deadlineAt: { type: string, format: date-time }
        refreezeRequestedAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        activeUsers: { type: integer, minimum: 0 }
        createdDays: { type: integer, minimum: 0 }
        terminalUsers: { oneOf: [{ type: integer, minimum: 0 }, { type: "null" }] }
        statusCounts:
          oneOf:
            - type: object
              additionalProperties: { type: integer, minimum: 0 }
            - { type: "null" }
        pool:
          type: object
          additionalProperties: false
          required: [candidateQuestionCount, unresolvedProgressNodeCount, remappedProgressNodeCount, invalidFixedQuestionCount, fixedQuestionCount, gapCount]
          properties:
            candidateQuestionCount: { type: integer, minimum: 0 }
            unresolvedProgressNodeCount: { type: integer, minimum: 0 }
            remappedProgressNodeCount: { type: integer, minimum: 0 }
            invalidFixedQuestionCount: { type: integer, minimum: 0 }
            fixedQuestionCount: { type: integer, minimum: 0 }
            gapCount: { type: integer, minimum: 0 }
    DailyPracticeCyclePage:
      type: object
      additionalProperties: false
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/DailyPracticeCycleSummary" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 50 }
    DailyPracticeCycleRefreezeResponse:
      type: object
      additionalProperties: false
      required: [practiceDate, status, refreezeRequestedAt, rebuildableDayCount, alreadyRequested]
      properties:
        practiceDate: { type: string, format: date }
        status: { $ref: "#/components/schemas/DailyPracticeCycleStatus" }
        refreezeRequestedAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        rebuildableDayCount: { type: integer, minimum: 0 }
        alreadyRequested: { type: boolean }
    AdminDailyPracticeUserSummary:
      type: object
      additionalProperties: false
      required: [id, displayName, role, status, initializationStatus, initializationProgress, todayStatus, generationSource, completed, summaryUpdatedAt, suggestionStatus]
      properties:
        id: { type: string }
        displayName: { type: string }
        role: { $ref: "#/components/schemas/Role" }
        status: { $ref: "#/components/schemas/AccountStatus" }
        initializationStatus: { $ref: "#/components/schemas/UserPracticeInitializationStatus" }
        initializationProgress: { type: number, minimum: 0, maximum: 100 }
        todayStatus:
          oneOf:
            - { $ref: "#/components/schemas/DailyPracticeDayStatus" }
            - { type: "null" }
        generationSource:
          oneOf:
            - { $ref: "#/components/schemas/DailyPracticeGenerationSource" }
            - { type: "null" }
        completed: { type: boolean }
        summaryUpdatedAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        suggestionStatus:
          oneOf:
            - { $ref: "#/components/schemas/DailyPracticeSuggestionStatus" }
            - { type: "null" }
    AdminDailyPracticeUserPage:
      type: object
      additionalProperties: false
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AdminDailyPracticeUserSummary" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1, maximum: 100 }
    AdminDailyPracticeStateEntry:
      type: object
      additionalProperties: false
      required: [id, subjectId, subject, label, masteryBps, attemptCount, wrongCount, nextReviewAt, lastPracticedAt]
      properties:
        id: { type: string }
        subjectId: { type: string }
        subject: { type: string }
        label: { type: string }
        masteryBps: { type: integer, minimum: 0, maximum: 10000 }
        attemptCount: { type: integer, minimum: 0 }
        wrongCount: { type: integer, minimum: 0 }
        nextReviewAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        lastPracticedAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
    AdminDailyPracticeCandidateQuestion:
      type: object
      additionalProperties: false
      required: [questionId, questionAlias, gradingType, typeLabel, promptExcerpt, knowledgeAliases, chapterAliases, priorityScore]
      properties:
        questionId: { type: string }
        questionAlias: { type: string }
        gradingType: { $ref: "#/components/schemas/QuestionType" }
        typeLabel: { type: string }
        promptExcerpt: { type: string, maxLength: 500 }
        knowledgeAliases:
          type: array
          items: { type: string }
        chapterAliases:
          type: array
          items: { type: string }
        priorityScore: { type: number }
    AdminDailyPracticeFrozenFixedQuestion:
      type: object
      additionalProperties: false
      required: [questionId, ordinal, gradingType, typeLabel, promptExcerpt, subjectId, subject, chapterIds, promptHash]
      properties:
        questionId: { type: string }
        ordinal: { type: integer, minimum: 1, maximum: 20 }
        gradingType: { $ref: "#/components/schemas/QuestionType" }
        typeLabel: { type: string }
        promptExcerpt: { type: string, maxLength: 500 }
        subjectId: { type: string }
        subject: { type: string }
        chapterIds:
          type: array
          items: { type: string }
        promptHash: { type: string, minLength: 64, maxLength: 64 }
    AdminDailyPracticeTodayPlan:
      type: object
      additionalProperties: false
      required: [dayId, practiceDate, profileRevision, progressSetHash, candidateHash, inputHash, outputHash, candidateQuestions, personalizedItems, fixedItems, fixedAssignment]
      properties:
        dayId: { type: string }
        practiceDate: { type: string, format: date }
        profileRevision: { type: integer, minimum: 0 }
        progressSetHash: { type: string, minLength: 64, maxLength: 64 }
        candidateHash: { oneOf: [{ type: string, minLength: 64, maxLength: 64 }, { type: "null" }] }
        inputHash: { oneOf: [{ type: string, minLength: 64, maxLength: 64 }, { type: "null" }] }
        outputHash: { oneOf: [{ type: string, minLength: 64, maxLength: 64 }, { type: "null" }] }
        candidateQuestions:
          type: array
          items: { $ref: "#/components/schemas/AdminDailyPracticeCandidateQuestion" }
        personalizedItems:
          type: array
          items: { $ref: "#/components/schemas/DailyPracticePlanItem" }
        fixedItems:
          type: array
          items: { $ref: "#/components/schemas/DailyPracticePlanItem" }
        fixedAssignment:
          oneOf:
            - type: object
              additionalProperties: false
              required: [assignmentId, hash, validCount, invalidCount, invalidQuestionIds, questions]
              properties:
                assignmentId: { oneOf: [{ type: string }, { type: "null" }] }
                hash: { type: string, minLength: 64, maxLength: 64 }
                validCount: { type: integer, minimum: 0, maximum: 20 }
                invalidCount: { type: integer, minimum: 0, maximum: 20 }
                invalidQuestionIds:
                  type: array
                  maxItems: 20
                  items: { type: string }
                questions:
                  type: array
                  maxItems: 20
                  items: { $ref: "#/components/schemas/AdminDailyPracticeFrozenFixedQuestion" }
            - { type: "null" }
    AdminDailyPracticeUserDetail:
      type: object
      additionalProperties: false
      required: [id, displayName, role, status, initializationStatus, initializationProgress, todayStatus, generationSource, completed, summaryUpdatedAt, suggestionStatus, profileRevision, summary, knowledgeStates, knowledgeStatesTotal, chapterStates, chapterStatesTotal, todayPlan, planRevisions, planRevisionsTotal, suggestions, suggestionsTotal]
      properties:
        id: { type: string }
        displayName: { type: string }
        role: { $ref: "#/components/schemas/Role" }
        status: { $ref: "#/components/schemas/AccountStatus" }
        initializationStatus: { $ref: "#/components/schemas/UserPracticeInitializationStatus" }
        initializationProgress: { type: number, minimum: 0, maximum: 100 }
        todayStatus:
          oneOf:
            - { $ref: "#/components/schemas/DailyPracticeDayStatus" }
            - { type: "null" }
        generationSource:
          oneOf:
            - { $ref: "#/components/schemas/DailyPracticeGenerationSource" }
            - { type: "null" }
        completed: { type: boolean }
        summaryUpdatedAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        suggestionStatus:
          oneOf:
            - { $ref: "#/components/schemas/DailyPracticeSuggestionStatus" }
            - { type: "null" }
        profileRevision: { type: integer, minimum: 0 }
        summary:
          oneOf:
            - { $ref: "#/components/schemas/DailyPracticeLearningSummary" }
            - { type: "null" }
        knowledgeStates:
          type: array
          maxItems: 100
          items: { $ref: "#/components/schemas/AdminDailyPracticeStateEntry" }
        knowledgeStatesTotal: { type: integer, minimum: 0 }
        chapterStates:
          type: array
          maxItems: 100
          items: { $ref: "#/components/schemas/AdminDailyPracticeStateEntry" }
        chapterStatesTotal: { type: integer, minimum: 0 }
        todayPlan:
          oneOf:
            - { $ref: "#/components/schemas/AdminDailyPracticeTodayPlan" }
            - { type: "null" }
        planRevisions:
          type: array
          maxItems: 50
          items:
            type: object
            additionalProperties: false
            required: [id, practiceDate, revision, trigger, generationSource, generatedAt, active]
            properties:
              id: { type: string }
              practiceDate: { type: string, format: date }
              revision: { type: integer, minimum: 1 }
              trigger: { $ref: "#/components/schemas/DailyPracticePlanTrigger" }
              generationSource: { $ref: "#/components/schemas/DailyPracticeGenerationSource" }
              generatedAt: { type: string, format: date-time }
              active: { type: boolean }
        planRevisionsTotal: { type: integer, minimum: 0 }
        suggestions:
          type: array
          maxItems: 50
          items: { $ref: "#/components/schemas/DailyPracticeSuggestion" }
        suggestionsTotal: { type: integer, minimum: 0 }
    AdminDailyPracticeSuggestionRequest:
      type: object
      additionalProperties: false
      required: [targetPracticeDate, intensity, desiredQuestionCount, focusSubjectIds, focusChapterIds]
      properties:
        targetPracticeDate: { type: string, format: date }
        intensity: { $ref: "#/components/schemas/DailyPracticeIntensity" }
        desiredQuestionCount: { type: integer, minimum: 5, maximum: 10 }
        focusSubjectIds:
          type: array
          maxItems: 2
          uniqueItems: true
          items: { type: string, minLength: 1, maxLength: 191 }
        focusChapterIds:
          type: array
          maxItems: 5
          uniqueItems: true
          items: { type: string, minLength: 1, maxLength: 191 }
        note: { type: string, minLength: 1, maxLength: 300 }
    AdminDailyPracticePlanAction:
      type: object
      additionalProperties: false
      required: [dayId, practiceDate, revision, trigger, status]
      properties:
        dayId: { type: string }
        practiceDate: { type: string, format: date }
        revision: { type: integer, minimum: 1 }
        trigger: { type: string, enum: [ADMIN_REGENERATE, ADMIN_PREVIEW] }
        status: { $ref: "#/components/schemas/DailyPracticeDayStatus" }
    AiQuestionGenerationComplexity:
      type: string
      enum: [SIMPLE, ASSOCIATIVE, COMPLEX, MAX]
    AiQuestionGenerationStatus:
      type: string
      enum: [PENDING, PROCESSING, COMPLETED, INVALID, FAILED, CANCELLED]
    AiQuestionGenerationRequest:
      type: object
      additionalProperties: false
      required: [subjectId, chapterIds, knowledgeNodeIds, gradingType, typeLabel, complexity, requestedCount]
      properties:
        subjectId: { type: string, minLength: 1, maxLength: 191 }
        chapterIds:
          type: array
          minItems: 1
          maxItems: 20
          uniqueItems: true
          items: { type: string }
        knowledgeNodeIds:
          type: array
          minItems: 1
          maxItems: 20
          uniqueItems: true
          description: 标题选择 ID；服务端将有子标题的节点展开为全部末级标题，并按展开后的末级证据执行 1-20 条限制
          items: { type: string }
        gradingType: { $ref: "#/components/schemas/QuestionType" }
        typeLabel: { type: string, minLength: 1, maxLength: 100 }
        complexity: { $ref: "#/components/schemas/AiQuestionGenerationComplexity" }
        requestedCount: { type: integer, minimum: 1, maximum: 20 }
    AiQuestionGenerationPreview:
      type: object
      required: [chapterCount, nodeCount, chunkCount, evidenceTokens, dedupCandidates, requestedCount, strategy, model, sourceRevision, promptVersion, maxOutputTokens]
      properties:
        chapterCount: { type: integer, minimum: 1, maximum: 20 }
        nodeCount: { type: integer, minimum: 1, maximum: 20 }
        chunkCount: { type: integer, minimum: 1, maximum: 80 }
        evidenceTokens: { type: integer, minimum: 1, maximum: 24000 }
        dedupCandidates: { type: integer, minimum: 0, maximum: 5000 }
        requestedCount: { type: integer, minimum: 1, maximum: 20 }
        strategy: { type: string, enum: [FLASH_NO_THINKING, FLASH_HIGH, PRO_HIGH, PRO_MAX] }
        model: { type: string }
        sourceRevision: { type: string }
        promptVersion: { type: string }
        maxOutputTokens: { type: integer, minimum: 1, maximum: 8000 }
    AiQuestionGenerationJob:
      type: object
      required: [id, subject, createdBy, gradingType, typeLabel, complexity, requestedCount, status, stage, promptVersion, attempts, createdAt, updatedAt, chapterIds, chapters, sourceCount, itemCount]
      properties:
        id: { type: string }
        subject: { $ref: "#/components/schemas/Subject" }
        createdBy: { $ref: "#/components/schemas/Author" }
        gradingType: { $ref: "#/components/schemas/QuestionType" }
        typeLabel: { type: string }
        complexity: { $ref: "#/components/schemas/AiQuestionGenerationComplexity" }
        requestedCount: { type: integer, minimum: 1, maximum: 20 }
        status: { $ref: "#/components/schemas/AiQuestionGenerationStatus" }
        stage: { type: string }
        promptVersion: { type: string }
        configSnapshot: { $ref: "#/components/schemas/AiQuestionGenerationPreview" }
        attempts: { type: integer, minimum: 0 }
        nextAttemptAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        cancelRequestedAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        errorCategory: { oneOf: [{ type: string }, { type: "null" }] }
        errorMessage: { oneOf: [{ type: string }, { type: "null" }] }
        completedAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        chapterIds:
          type: array
          items: { type: string }
        chapters:
          type: array
          items: { $ref: "#/components/schemas/SubjectChapter" }
        sourceCount: { type: integer, minimum: 0 }
        itemCount: { type: integer, minimum: 0 }
    AiQuestionGenerationJobPage:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AiQuestionGenerationJob" }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1 }
    AiQuestionReviewSummary:
      type: object
      required: [id, type, typeLabel, subjectId, subject, chapterIds, chapters, prompt, reviewStatus, reviewRevision, sourceReviewStatus, author, createdAt, updatedAt]
      properties:
        id: { type: string }
        type: { $ref: "#/components/schemas/QuestionType" }
        typeLabel: { type: string }
        subjectId: { type: string }
        subject: { $ref: "#/components/schemas/Subject" }
        chapterIds: { type: array, items: { type: string } }
        chapters: { type: array, items: { $ref: "#/components/schemas/SubjectChapter" } }
        prompt: { type: string }
        reviewStatus: { type: string, enum: [DRAFT_REVIEW, APPROVED, REJECTED] }
        reviewRevision: { type: integer, minimum: 1 }
        sourceReviewStatus: { type: string, enum: [NOT_APPLICABLE, VALID, REVIEW_REQUIRED] }
        sourceReviewRequiredAt: { oneOf: [{ type: string, format: date-time }, { type: "null" }] }
        author: { $ref: "#/components/schemas/Author" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    AiQuestionReviewPage:
      type: object
      required: [items, total, page, pageSize]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/AiQuestionReviewSummary" } }
        total: { type: integer, minimum: 0 }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 1 }
    AiQuestionReviewDetail:
      allOf:
        - { $ref: "#/components/schemas/AiQuestionReviewSummary" }
        - type: object
          required: [gradingType, options, correctAnswer, maxScore, explanation, sourceRevision, originalGenerated, sources, events]
          properties:
            gradingType: { $ref: "#/components/schemas/QuestionType" }
            options: { type: array, items: { $ref: "#/components/schemas/QuizOption" } }
            correctAnswer: { type: array, items: { type: string } }
            gradingRubric: { oneOf: [{ type: object, additionalProperties: true }, { type: "null" }] }
            maxScore: { type: integer, minimum: 1 }
            explanation: { type: string }
            sourceRevision: { type: integer, minimum: 1 }
            originalGenerated: {}
            sources:
              type: array
              items:
                type: object
                required: [id, sourceRevision, ordinal, current, libraryId, documentId, documentVersionId, libraryChapterId, title, breadcrumb, contentHash, evidenceContent]
                properties:
                  id: { type: string }
                  sourceRevision: { type: integer, minimum: 1 }
                  ordinal: { type: integer, minimum: 0 }
                  current: { type: boolean }
                  knowledgeNodeId: { oneOf: [{ type: string }, { type: "null" }] }
                  libraryId: { type: string }
                  documentId: { type: string }
                  documentVersionId: { type: string }
                  libraryChapterId: { type: string }
                  title: { type: string }
                  breadcrumb: { type: string }
                  contentHash: { type: string }
                  evidenceContent: { type: string }
            events: { type: array, items: { type: object, additionalProperties: true } }
    AiQuestionReviewWriteRequest:
      type: object
      additionalProperties: false
      required: [expectedRevision, typeLabel, chapterIds, prompt, options, correctAnswer, explanation]
      properties:
        expectedRevision: { type: integer, minimum: 1 }
        typeLabel: { type: string, minLength: 1, maxLength: 100 }
        chapterIds: { type: array, minItems: 1, maxItems: 20, uniqueItems: true, items: { type: string } }
        prompt: { type: string, minLength: 2, maxLength: 10000 }
        options: { type: array, maxItems: 20, items: { $ref: "#/components/schemas/QuizOption" } }
        correctAnswer: { type: array, minItems: 1, maxItems: 20, items: { type: string } }
        gradingRubric: { oneOf: [{ type: object, additionalProperties: true }, { type: "null" }] }
        explanation: { type: string, maxLength: 10000 }
    AiQuestionRejectRequest:
      type: object
      additionalProperties: false
      required: [expectedRevision, reason]
      properties:
        expectedRevision: { type: integer, minimum: 1 }
        reason: { type: string, minLength: 1, maxLength: 1000 }
    AiQuestionReopenRequest:
      type: object
      additionalProperties: false
      required: [expectedRevision]
      properties:
        expectedRevision: { type: integer, minimum: 1 }
    AiQuestionSourceRevalidateRequest:
      type: object
      additionalProperties: false
      required: [expectedRevision, knowledgeNodeIds]
      properties:
        expectedRevision: { type: integer, minimum: 1 }
        knowledgeNodeIds: { type: array, minItems: 1, maxItems: 20, uniqueItems: true, items: { type: string } }
    AiQuestionSourceBulkRevalidateRequest:
      type: object
      additionalProperties: false
      required: [questionIds]
      properties:
        questionIds: { type: array, minItems: 1, maxItems: 50, items: { type: string, minLength: 1, maxLength: 191 } }
    AiQuestionSourceBulkRevalidateResult:
      type: object
      additionalProperties: false
      required: [questionId, status]
      properties:
        questionId: { type: string }
        status: { type: string, enum: [REVALIDATED, SKIPPED, FAILED] }
        reason: { type: string }
        sourceRevision: { type: integer, minimum: 1 }
    AiQuestionSourceBulkRevalidateResponse:
      type: object
      additionalProperties: false
      required: [results]
      properties:
        results:
          type: array
          maxItems: 50
          items: { $ref: "#/components/schemas/AiQuestionSourceBulkRevalidateResult" }
